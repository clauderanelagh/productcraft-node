{
  "components": {
    "schemas": {
      "AcceptWorkspaceInviteDto": {
        "properties": {
          "code": {
            "description": "Invite code from the invite email / link.",
            "example": "inv_…",
            "type": "string"
          }
        },
        "required": [
          "code"
        ],
        "type": "object"
      },
      "AssertionAuthorizationRequestDto": {
        "properties": {
          "action": {
            "description": "Action to decide, `<service>.<resource>.<verb>`.",
            "type": "string"
          },
          "resource": {
            "description": "URN the action targets. Omitted means `*`, the strict reading.",
            "type": "string"
          }
        },
        "required": [
          "action"
        ],
        "type": "object"
      },
      "AuditFeedResponseDto": {
        "properties": {
          "data": {
            "items": {
              "$ref": "#/components/schemas/FeedItemDto"
            },
            "type": "array"
          },
          "pagination": {
            "$ref": "#/components/schemas/PaginationDto"
          },
          "sources": {
            "description": "Per-source diagnostics so the UI / customer can see which sources contributed to this page and which are stubbed pending the cross-service fanout.",
            "items": {
              "$ref": "#/components/schemas/SourceDiagnosticDto"
            },
            "type": "array"
          }
        },
        "required": [
          "data",
          "pagination",
          "sources"
        ],
        "type": "object"
      },
      "AuthorizationServerMetadataDto": {
        "properties": {
          "authority_model": {
            "description": "Non-standard. Declares that authority on this server is an IAM policy document stored on the grant, intersected per request with the authorizing human’s current workspace policy — so a demotion or an offboarding takes effect on the next call rather than at token expiry. The `scope` parameter only names a preset the human then narrows.",
            "example": "policy",
            "type": "string"
          },
          "authorization_endpoint": {
            "description": "RFC 6749 authorization endpoint.",
            "type": "string"
          },
          "code_challenge_methods_supported": {
            "description": "S256 only. `plain` is rejected, and an authorization request without a challenge is rejected.",
            "example": [
              "S256"
            ],
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "grant_types_supported": {
            "example": [
              "authorization_code",
              "refresh_token"
            ],
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "introspection_endpoint": {
            "description": "RFC 7662 token introspection endpoint.",
            "type": "string"
          },
          "issuer": {
            "description": "Issuer identifier. Matches the `iss` claim on every access token this server mints.",
            "example": "https://api.platform-auth.productcraft.co",
            "type": "string"
          },
          "jwks_uri": {
            "description": "JWKS the access-token signature verifies against.",
            "type": "string"
          },
          "registration_endpoint": {
            "description": "RFC 7591 dynamic client registration endpoint.",
            "type": "string"
          },
          "resource_indicators_supported": {
            "description": "RFC 8707 resource indicators this server will mint an audience for.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "response_types_supported": {
            "description": "Only `code` — the implicit and password grants do not exist here.",
            "example": [
              "code"
            ],
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "revocation_endpoint": {
            "description": "RFC 7009 token revocation endpoint.",
            "type": "string"
          },
          "scope_descriptions": {
            "additionalProperties": {
              "type": "string"
            },
            "description": "Plain-language description per preset. Non-standard, published so a client can render the same wording the consent screen uses.",
            "type": "object"
          },
          "scopes_supported": {
            "description": "Preset names accepted in the `scope` parameter. A preset is a policy *template*, never authority in itself — see `authority_model`. Ordinary OAuth scopes do not exist on this server.",
            "example": [
              "readonly",
              "mail",
              "support",
              "full"
            ],
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "service_documentation": {
            "description": "Human-readable documentation for this authorization server.",
            "type": "string"
          },
          "token_endpoint": {
            "description": "RFC 6749 token endpoint.",
            "type": "string"
          },
          "token_endpoint_auth_methods_supported": {
            "example": [
              "none",
              "client_secret_basic",
              "client_secret_post"
            ],
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "required": [
          "issuer",
          "authorization_endpoint",
          "token_endpoint",
          "registration_endpoint",
          "revocation_endpoint",
          "introspection_endpoint",
          "jwks_uri",
          "response_types_supported",
          "grant_types_supported",
          "code_challenge_methods_supported",
          "token_endpoint_auth_methods_supported",
          "scopes_supported",
          "scope_descriptions",
          "authority_model",
          "resource_indicators_supported",
          "service_documentation"
        ],
        "type": "object"
      },
      "AvatarUrlResponseDto": {
        "properties": {
          "avatar_url": {
            "type": "string"
          }
        },
        "required": [
          "avatar_url"
        ],
        "type": "object"
      },
      "BoundPolicySummaryDto": {
        "properties": {
          "description": {
            "nullable": true,
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "name": {
            "example": "mail-readonly",
            "type": "string"
          }
        },
        "required": [
          "id",
          "name",
          "description"
        ],
        "type": "object"
      },
      "CallbackBodyDto": {
        "properties": {
          "code": {
            "description": "OAuth authorization code returned by the IdP. Optional — either `idToken` or `code` must be supplied.",
            "maxLength": 2048,
            "type": "string"
          },
          "id_token": {
            "description": "JWT id_token from the IdP (OIDC providers; Apple form_post). Optional — either `idToken` or `code` must be supplied.",
            "maxLength": 8192,
            "type": "string"
          },
          "state": {
            "description": "Opaque state token returned by the IdP, minted at `/start`. Single-use; binds the callback to a specific browser session.",
            "maxLength": 512,
            "type": "string"
          },
          "user": {
            "description": "Apple-only first-signin payload: JSON-encoded `{ name, email }`. Apple returns this only the first time a user signs in. Not signed — treated as untrusted input by the controller's payload parser.",
            "maxLength": 4096,
            "type": "string"
          }
        },
        "required": [
          "state"
        ],
        "type": "object"
      },
      "ClientMetadataDto": {
        "properties": {
          "client_id": {
            "example": "pcft_cli_2f5c...",
            "type": "string"
          },
          "client_id_issued_at": {
            "description": "Unix seconds at which the client_id was issued.",
            "type": "number"
          },
          "client_name": {
            "type": "string"
          },
          "client_secret_expires_at": {
            "description": "0 — the client secret does not expire on its own.",
            "example": 0,
            "type": "number"
          },
          "grant_types": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "is_verified": {
            "description": "Always false for a dynamically registered client. The consent screen shows this to the human.",
            "example": false,
            "type": "boolean"
          },
          "redirect_uris": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "registration_client_uri": {
            "description": "Endpoint the registration access token is used against.",
            "type": "string"
          },
          "response_types": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "scope": {
            "type": "string"
          },
          "token_endpoint_auth_method": {
            "type": "string"
          }
        },
        "required": [
          "client_id",
          "client_id_issued_at",
          "client_secret_expires_at",
          "registration_client_uri",
          "redirect_uris",
          "client_name",
          "grant_types",
          "response_types",
          "token_endpoint_auth_method",
          "scope",
          "is_verified"
        ],
        "type": "object"
      },
      "ClientRegistrationRequestDto": {
        "properties": {
          "client_name": {
            "description": "Display name shown on the consent screen next to an \"unverified\" badge. Control characters are stripped; the client cannot supply any other consent-screen copy.",
            "example": "Claude Desktop",
            "maxLength": 120,
            "type": "string"
          },
          "client_uri": {
            "description": "Absolute https homepage for the client.",
            "nullable": true,
            "type": "string"
          },
          "grant_types": {
            "description": "Must include `authorization_code`. `refresh_token` is the only other supported value.",
            "example": [
              "authorization_code",
              "refresh_token"
            ],
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "logo_uri": {
            "description": "Absolute https logo URL. Stored but deliberately never rendered on the consent screen — a client-supplied image is client-supplied display text by another name.",
            "nullable": true,
            "type": "string"
          },
          "redirect_uris": {
            "description": "Exact redirect URIs. Matched byte-for-byte at `/authorize`; the only relaxation is the RFC 8252 §7.3 loopback port. `https` anywhere, `http` on `127.0.0.1` / `[::1]` only, or a reverse-DNS private-use scheme. No wildcards, no fragments.",
            "example": [
              "http://127.0.0.1:33418/callback"
            ],
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "response_types": {
            "description": "Only `code` is supported.",
            "example": [
              "code"
            ],
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "scope": {
            "description": "Space-delimited ceiling of scopes this client may ever request. Defaults to the full published scope list; the human still approves each one.",
            "example": "workspace:read mail:read mail:send",
            "type": "string"
          },
          "token_endpoint_auth_method": {
            "description": "`none` for a public client (PKCE only), or `client_secret_basic` / `client_secret_post` for a confidential client. Defaults to `none`.",
            "example": "none",
            "type": "string"
          }
        },
        "required": [
          "redirect_uris",
          "client_name"
        ],
        "type": "object"
      },
      "ClientRegistrationResponseDto": {
        "properties": {
          "client_id": {
            "example": "pcft_cli_2f5c...",
            "type": "string"
          },
          "client_id_issued_at": {
            "description": "Unix seconds at which the client_id was issued.",
            "type": "number"
          },
          "client_name": {
            "type": "string"
          },
          "client_secret": {
            "description": "Returned exactly once, and only for confidential clients. Stored hashed; it cannot be retrieved again.",
            "nullable": true,
            "type": "string"
          },
          "client_secret_expires_at": {
            "description": "0 — the client secret does not expire on its own.",
            "example": 0,
            "type": "number"
          },
          "grant_types": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "is_verified": {
            "description": "Always false for a dynamically registered client. The consent screen shows this to the human.",
            "example": false,
            "type": "boolean"
          },
          "redirect_uris": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "registration_access_token": {
            "description": "Bearer token that reads and updates THIS registration and nothing else. Returned once.",
            "type": "string"
          },
          "registration_client_uri": {
            "description": "Endpoint the registration access token is used against.",
            "type": "string"
          },
          "response_types": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "scope": {
            "type": "string"
          },
          "token_endpoint_auth_method": {
            "type": "string"
          }
        },
        "required": [
          "client_id",
          "client_id_issued_at",
          "client_secret_expires_at",
          "registration_client_uri",
          "redirect_uris",
          "client_name",
          "grant_types",
          "response_types",
          "token_endpoint_auth_method",
          "scope",
          "is_verified",
          "client_secret",
          "registration_access_token"
        ],
        "type": "object"
      },
      "ConnectedClientDto": {
        "properties": {
          "account_id": {
            "description": "The human who consented to this grant.",
            "format": "uuid",
            "type": "string"
          },
          "client_id": {
            "type": "string"
          },
          "client_name": {
            "type": "string"
          },
          "created_at": {
            "format": "date-time",
            "type": "string"
          },
          "grant_id": {
            "format": "uuid",
            "type": "string"
          },
          "is_verified": {
            "type": "boolean"
          },
          "policy": {
            "description": "Policy the human approved. Effective authority is this intersected with the human's current workspace policy, per request.",
            "items": {
              "$ref": "#/components/schemas/PolicyStatementDto"
            },
            "type": "array"
          },
          "preset": {
            "description": "Preset the grant started from, when it started from one.",
            "example": "mail",
            "nullable": true,
            "type": "string"
          },
          "resource": {
            "type": "string"
          },
          "revoked_at": {
            "format": "date-time",
            "nullable": true,
            "type": "string"
          },
          "workspace_id": {
            "format": "uuid",
            "type": "string"
          }
        },
        "required": [
          "grant_id",
          "client_id",
          "client_name",
          "is_verified",
          "account_id",
          "workspace_id",
          "preset",
          "policy",
          "resource",
          "created_at",
          "revoked_at"
        ],
        "type": "object"
      },
      "ConnectedClientListDto": {
        "properties": {
          "data": {
            "items": {
              "$ref": "#/components/schemas/ConnectedClientDto"
            },
            "type": "array"
          },
          "pagination": {
            "$ref": "#/components/schemas/PaginationDto"
          }
        },
        "required": [
          "data",
          "pagination"
        ],
        "type": "object"
      },
      "ConsentDecisionDto": {
        "properties": {
          "approved": {
            "description": "True to approve, false to deny.",
            "type": "boolean"
          },
          "policy": {
            "description": "The policy the human approved. Defaults to the policy the requested presets resolve to. May narrow it, and may add an escalation action a preset withheld — but never beyond the human's own authority: a grant that exceeds the granter is refused with 403 and the response names the refused statements.",
            "items": {
              "$ref": "#/components/schemas/PolicyStatementDto"
            },
            "type": "array"
          },
          "workspace_id": {
            "description": "Workspace the grant is scoped to. Required when approving.",
            "format": "uuid",
            "type": "string"
          }
        },
        "required": [
          "approved"
        ],
        "type": "object"
      },
      "ConsentDecisionResponseDto": {
        "properties": {
          "redirect_to": {
            "description": "Absolute URL the browser must be sent to. Always the redirect URI captured when the request was created.",
            "type": "string"
          }
        },
        "required": [
          "redirect_to"
        ],
        "type": "object"
      },
      "ConsentPresetDto": {
        "properties": {
          "description": {
            "description": "Server-authored plain-language wording. The client cannot influence it.",
            "example": "Read and send email from your verified domains.",
            "type": "string"
          },
          "preset": {
            "description": "Preset name the client asked for. A preset is a policy *template*, never authority in itself.",
            "enum": [
              "readonly",
              "mail",
              "support",
              "full"
            ],
            "example": "mail",
            "type": "string"
          }
        },
        "required": [
          "preset",
          "description"
        ],
        "type": "object"
      },
      "ConsentRequestDto": {
        "properties": {
          "client_id": {
            "type": "string"
          },
          "client_name": {
            "description": "Display name the client registered with.",
            "type": "string"
          },
          "client_uri": {
            "nullable": true,
            "type": "string"
          },
          "escalation_actions": {
            "description": "Escalation actions (credential minting, role/policy editing, m2m issuance) the requested policy would grant. Always empty for a preset; the consent screen must surface any entry here in its own section, not among ordinary permissions.",
            "example": [],
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "expires_at": {
            "format": "date-time",
            "type": "string"
          },
          "is_verified": {
            "description": "False for every dynamically registered client. The screen must show this prominently.",
            "type": "boolean"
          },
          "presets": {
            "description": "Presets the client requested. The consent screen renders these in one line each.",
            "items": {
              "$ref": "#/components/schemas/ConsentPresetDto"
            },
            "type": "array"
          },
          "request_id": {
            "format": "uuid",
            "type": "string"
          },
          "requested_policy": {
            "description": "The policy the presets resolve to — the starting point the human narrows. Authority is whatever they approve.",
            "items": {
              "$ref": "#/components/schemas/PolicyStatementDto"
            },
            "type": "array"
          },
          "resource": {
            "description": "RFC 8707 resource the token will be audience-bound to.",
            "type": "string"
          },
          "workspaces": {
            "description": "Workspaces the human may pick from. Access is scoped to exactly one.",
            "items": {
              "$ref": "#/components/schemas/ConsentWorkspaceDto"
            },
            "type": "array"
          }
        },
        "required": [
          "request_id",
          "client_id",
          "client_name",
          "client_uri",
          "is_verified",
          "presets",
          "requested_policy",
          "escalation_actions",
          "resource",
          "workspaces",
          "expires_at"
        ],
        "type": "object"
      },
      "ConsentWorkspaceDto": {
        "properties": {
          "display_name": {
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "role": {
            "description": "Caller's role in this workspace.",
            "example": "owner",
            "type": "string"
          },
          "slug": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "slug",
          "display_name",
          "role"
        ],
        "type": "object"
      },
      "CreatePakDto": {
        "properties": {
          "description": {
            "description": "Optional description (e.g. which service uses the key).",
            "maxLength": 500,
            "type": "string"
          },
          "name": {
            "description": "Human-readable PAK name (shown in the keys list).",
            "example": "Production social client",
            "maxLength": 120,
            "minLength": 1,
            "type": "string"
          },
          "policy_ids": {
            "description": "IDs of managed policies (workspace_policy rows) to bind to this PAK. Bind one or more — the PAK's effective policy is the union of all bound statements.",
            "example": [
              "11111111-1111-1111-1111-111111111111"
            ],
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "required": [
          "name",
          "policy_ids"
        ],
        "type": "object"
      },
      "CreateWorkspaceDto": {
        "properties": {
          "display_name": {
            "description": "Display name shown in console and on receipts.",
            "example": "Acme Corp",
            "maxLength": 128,
            "minLength": 1,
            "type": "string"
          },
          "slug": {
            "description": "URL-friendly slug. Lowercase alphanumerics, separated by hyphens. Used in URLs and as a stable handle.",
            "example": "acme-corp",
            "maxLength": 64,
            "minLength": 2,
            "type": "string"
          }
        },
        "required": [
          "slug",
          "display_name"
        ],
        "type": "object"
      },
      "CreateWorkspaceInviteDto": {
        "properties": {
          "email": {
            "description": "Optional invitee email. When set, the invite is locked to that address.",
            "example": "alice@example.com",
            "type": "string"
          },
          "expires_in_hours": {
            "description": "Hours until the invite expires. Defaults to 168 (7 days).",
            "example": 168,
            "maximum": 8760,
            "minimum": 1,
            "type": "number"
          },
          "max_uses": {
            "description": "Maximum number of accepts. Defaults to 1. Use higher values for \"team link\" invites.",
            "example": 1,
            "maximum": 10000,
            "minimum": 1,
            "type": "number"
          },
          "role": {
            "description": "System role shortcut: 'owner' | 'admin' | 'member'.",
            "enum": [
              "owner",
              "admin",
              "member"
            ],
            "type": "string"
          },
          "role_id": {
            "description": "UUID of the role to grant on accept. Use to target custom roles.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "CreateWorkspacePolicyDto": {
        "properties": {
          "description": {
            "description": "Optional human description. Pass `null` to clear an existing description on PATCH.",
            "example": "Read-only access to all Mail resources",
            "nullable": true,
            "type": "string"
          },
          "name": {
            "description": "Policy name (1-64 chars, alphanumeric + space/underscore/hyphen). Unique per workspace.",
            "example": "mail-readonly",
            "maxLength": 64,
            "minLength": 1,
            "type": "string"
          },
          "policy": {
            "description": "IAM-style policy. Empty array means \"no permissions\" — useful as a placeholder before granting any actions.",
            "example": [
              {
                "actions": [
                  "mail.read",
                  "mail.list"
                ],
                "effect": "allow",
                "resources": [
                  "*"
                ]
              }
            ],
            "items": {
              "$ref": "#/components/schemas/PolicyStatementDto"
            },
            "type": "array"
          }
        },
        "required": [
          "name",
          "policy"
        ],
        "type": "object"
      },
      "CreateWorkspaceRoleDto": {
        "properties": {
          "description": {
            "description": "Optional human-readable role description.",
            "maxLength": 256,
            "type": "string"
          },
          "name": {
            "description": "Role name. Free-form; not URL-significant.",
            "example": "BillingAdmin",
            "maxLength": 64,
            "minLength": 1,
            "type": "string"
          },
          "policy": {
            "description": "IAM-style policy granting the role its permissions. Same wire shape as PAK policies. Omit or pass `[]` for a no-permissions role.",
            "example": [
              {
                "actions": [
                  "mail.*.read"
                ],
                "effect": "allow",
                "resources": [
                  "*"
                ]
              }
            ],
            "items": {
              "$ref": "#/components/schemas/PolicyStatementDto"
            },
            "type": "array"
          },
          "policy_id": {
            "description": "Optional managed-policy id to bind the role to. When set, the role's effective policy comes from the bound managed policy and `policy` (inline) must be omitted.",
            "example": "11111111-1111-1111-1111-111111111111",
            "nullable": true,
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "type": "object"
      },
      "DelegatedActionDecisionDto": {
        "properties": {
          "action": {
            "description": "The action that was decided.",
            "example": "mail.send",
            "type": "string"
          },
          "allowed": {
            "description": "Whether the effective policy allows this action at the requested resource right now.",
            "type": "boolean"
          }
        },
        "required": [
          "action",
          "allowed"
        ],
        "type": "object"
      },
      "DelegatedAuthorizationBatchRequestDto": {
        "properties": {
          "actions": {
            "description": "Actions to decide, in `<service>.<resource>.<verb>` form. Duplicates are evaluated once and echoed per entry.",
            "example": [
              "mail.send",
              "mail.list"
            ],
            "items": {
              "type": "string"
            },
            "maxItems": 500,
            "type": "array"
          },
          "resource": {
            "description": "Resource URN every action is decided against. Omitted means `*`, which the human’s URN-narrowed denies still constrain.",
            "example": "pcft:envoi:domain/8f14e45f-ceea-467a-9c1a-2b8a4f2a11c3",
            "maxLength": 500,
            "type": "string"
          }
        },
        "required": [
          "actions"
        ],
        "type": "object"
      },
      "DelegatedAuthorizationBatchResponseDto": {
        "properties": {
          "decisions": {
            "description": "One decision per requested action, in the order they were requested.",
            "items": {
              "$ref": "#/components/schemas/DelegatedActionDecisionDto"
            },
            "type": "array"
          },
          "reason": {
            "description": "Machine-readable outcome for the token as a whole: `evaluated`, `token_inactive`, `token_expired`, `grant_revoked`, `granter_not_a_member`, or `granter_account_inactive`. Anything but `evaluated` means every decision is `false`.",
            "example": "evaluated",
            "type": "string"
          }
        },
        "required": [
          "decisions",
          "reason"
        ],
        "type": "object"
      },
      "DelegatedAuthorizationRequestDto": {
        "properties": {
          "action": {
            "description": "The action to decide, in `<service>.<resource>.<verb>` form — the same catalogue the workspace-role and PAK lanes evaluate against.",
            "example": "mail.message.send",
            "maxLength": 200,
            "type": "string"
          },
          "resource": {
            "description": "Resource URN the action targets. Omitted means `*`, which the human’s URN-narrowed denies still constrain.",
            "example": "pcft:envoi:domain/8f14e45f-ceea-467a-9c1a-2b8a4f2a11c3",
            "maxLength": 500,
            "type": "string"
          }
        },
        "required": [
          "action"
        ],
        "type": "object"
      },
      "DelegatedAuthorizationResponseDto": {
        "properties": {
          "allowed": {
            "description": "Whether the effective policy — the grant’s policy AND the authorizing human’s current workspace policy — allows this action right now.",
            "type": "boolean"
          },
          "reason": {
            "description": "Machine-readable outcome: `allowed`, `denied_by_effective_policy`, `token_inactive`, `token_expired`, `grant_revoked`, `granter_not_a_member`, or `granter_account_inactive` (the authorizing human’s account is suspended or deactivated).",
            "example": "allowed",
            "type": "string"
          }
        },
        "required": [
          "allowed",
          "reason"
        ],
        "type": "object"
      },
      "DownstreamAssertionRequestDto": {
        "properties": {
          "audience": {
            "description": "Hostname of the service the assertion is for, e.g. `api.mail.productcraft.co`. Must be one we issue for; anything else is 400. This is an allowlist and not a free-text audience, because the audience string is what separates this family from the session lane.",
            "example": "api.mail.productcraft.co",
            "type": "string"
          }
        },
        "required": [
          "audience"
        ],
        "type": "object"
      },
      "DownstreamAssertionResponseDto": {
        "properties": {
          "assertion": {
            "description": "The signed assertion. Verifies offline against the platform JWKS. Carries identity only — no policy.",
            "type": "string"
          },
          "audience": {
            "description": "The audience it was minted for, echoed back.",
            "example": "api.mail.productcraft.co",
            "type": "string"
          },
          "expires_in": {
            "description": "Seconds until it expires. Deliberately short.",
            "example": 60,
            "type": "number"
          }
        },
        "required": [
          "assertion",
          "expires_in",
          "audience"
        ],
        "type": "object"
      },
      "EnableWorkspaceServiceDto": {
        "properties": {
          "settings": {
            "description": "Optional per-service settings blob captured at enable time.",
            "type": "object"
          }
        },
        "type": "object"
      },
      "FeedItemDto": {
        "properties": {
          "action": {
            "example": "workspace.member.removed",
            "type": "string"
          },
          "actor_id": {
            "format": "uuid",
            "nullable": true,
            "type": "string"
          },
          "actor_type": {
            "description": "Actor classification — one of 'platform_user' | 'end_user' | 'm2m' | 'system' | 'pat' | 'api_key'.",
            "example": "platform_user",
            "type": "string"
          },
          "metadata": {
            "description": "Free-form metadata (jsonb).",
            "type": "object"
          },
          "resource": {
            "example": "workspace_membership",
            "type": "string"
          },
          "resource_id": {
            "format": "uuid",
            "nullable": true,
            "type": "string"
          },
          "source": {
            "description": "Which audit table this row came from.",
            "enum": [
              "workspace_audit",
              "heimdall_audit",
              "agora_moderation"
            ],
            "example": "workspace_audit",
            "type": "string"
          },
          "source_ref": {
            "description": "Stable per-source identifier. Used by the cursor to resume from the same point on the next page.",
            "example": "4e5f5c9e-b4ee-4401-9275-283feb66c178",
            "type": "string"
          },
          "timestamp": {
            "format": "date-time",
            "type": "string"
          }
        },
        "required": [
          "source",
          "timestamp",
          "actor_id",
          "actor_type",
          "action",
          "resource",
          "resource_id",
          "metadata",
          "source_ref"
        ],
        "type": "object"
      },
      "FeedbackResponseDto": {
        "properties": {
          "message": {
            "description": "Human-readable message",
            "example": "Email verified successfully",
            "type": "string"
          }
        },
        "required": [
          "message"
        ],
        "type": "object"
      },
      "IntrospectAccountDto": {
        "properties": {
          "avatar_url": {
            "description": "Public HTTPS URL of the uploaded avatar, or null when no avatar is set.",
            "nullable": true,
            "type": "string"
          },
          "display_name": {
            "nullable": true,
            "type": "string"
          },
          "email": {
            "nullable": true,
            "type": "string"
          },
          "email_verified_at": {
            "description": "Timestamp when this account verified its primary email, or null if unverified.",
            "format": "date-time",
            "nullable": true,
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "status": {
            "description": "Account status (e.g. `active`, `suspended`).",
            "type": "string"
          },
          "username": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "email",
          "username",
          "display_name",
          "status",
          "email_verified_at",
          "avatar_url"
        ],
        "type": "object"
      },
      "IntrospectApiKeyResponseDto": {
        "properties": {
          "api_key_id": {
            "format": "uuid",
            "type": "string"
          },
          "created_by": {
            "format": "uuid",
            "nullable": true,
            "type": "string"
          },
          "is_valid": {
            "type": "boolean"
          },
          "policy": {
            "description": "Merged effective policy across every managed policy bound to this PAK.",
            "items": {
              "$ref": "#/components/schemas/PolicyStatementDto"
            },
            "type": "array"
          },
          "workspace_id": {
            "format": "uuid",
            "type": "string"
          }
        },
        "required": [
          "is_valid",
          "workspace_id",
          "policy",
          "created_by",
          "api_key_id"
        ],
        "type": "object"
      },
      "IntrospectPrincipalDto": {
        "properties": {
          "email": {
            "nullable": true,
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "name": {
            "nullable": true,
            "type": "string"
          },
          "sub": {
            "format": "uuid",
            "type": "string"
          }
        },
        "required": [
          "id",
          "sub",
          "email",
          "name"
        ],
        "type": "object"
      },
      "IntrospectResponseDto": {
        "properties": {
          "account": {
            "allOf": [
              {
                "$ref": "#/components/schemas/IntrospectAccountDto"
              }
            ],
            "nullable": true
          },
          "is_authenticated": {
            "type": "boolean"
          },
          "principal": {
            "allOf": [
              {
                "$ref": "#/components/schemas/IntrospectPrincipalDto"
              }
            ],
            "nullable": true
          },
          "workspaces": {
            "items": {
              "$ref": "#/components/schemas/IntrospectWorkspaceListItemDto"
            },
            "type": "array"
          }
        },
        "required": [
          "is_authenticated"
        ],
        "type": "object"
      },
      "IntrospectRoleDto": {
        "properties": {
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "name": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "name"
        ],
        "type": "object"
      },
      "IntrospectWorkspaceListItemDto": {
        "properties": {
          "avatar_url": {
            "description": "Workspace identity avatar (public HTTPS URL), or null when none is set. Console workspace switcher reads this.",
            "nullable": true,
            "type": "string"
          },
          "display_name": {
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "role": {
            "description": "Caller’s role name in this workspace.",
            "example": "owner",
            "type": "string"
          },
          "services": {
            "description": "Enabled services on this workspace.",
            "example": [
              "mail",
              "waitlist"
            ],
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "slug": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "slug",
          "display_name",
          "role",
          "services",
          "avatar_url"
        ],
        "type": "object"
      },
      "IntrospectWorkspaceResponseDto": {
        "properties": {
          "id": {
            "description": "Canonical workspace UUID. Only present for members — non-members get a uniform low-information response.",
            "format": "uuid",
            "type": "string"
          },
          "is_member": {
            "type": "boolean"
          },
          "policy": {
            "description": "Effective IAM-style policy for this caller in this workspace.",
            "items": {
              "$ref": "#/components/schemas/PolicyStatementDto"
            },
            "type": "array"
          },
          "role": {
            "allOf": [
              {
                "$ref": "#/components/schemas/IntrospectRoleDto"
              }
            ],
            "nullable": true
          },
          "services": {
            "description": "Enabled services on this workspace.",
            "example": [
              "mail",
              "waitlist"
            ],
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "required": [
          "is_member",
          "role",
          "policy",
          "services"
        ],
        "type": "object"
      },
      "IntrospectionRequestDto": {
        "properties": {
          "client_id": {
            "type": "string"
          },
          "client_secret": {
            "type": "string"
          },
          "token": {
            "description": "The access token to introspect.",
            "type": "string"
          }
        },
        "required": [
          "token"
        ],
        "type": "object"
      },
      "IntrospectionResponseDto": {
        "properties": {
          "active": {
            "description": "False for an unknown, expired, revoked, or foreign token — the four are deliberately indistinguishable.",
            "type": "boolean"
          },
          "aud": {
            "description": "Resource indicator the token is bound to.",
            "type": "string"
          },
          "client_id": {
            "type": "string"
          },
          "effective_policy": {
            "description": "Effective authority: the token's policy intersected with the human's CURRENT workspace policy, recomputed on every call with no cache. An action the human's role loses disappears here without anyone revoking the token; a human who left the workspace makes the token report `active: false`. This is a report — the authorization decision itself re-evaluates both policies per (action, resource) cell.",
            "items": {
              "$ref": "#/components/schemas/PolicyStatementDto"
            },
            "type": "array"
          },
          "exp": {
            "description": "Expiry, unix seconds.",
            "type": "number"
          },
          "policy": {
            "description": "The policy stored on the token — the ceiling the human approved at consent.",
            "items": {
              "$ref": "#/components/schemas/PolicyStatementDto"
            },
            "type": "array"
          },
          "scope": {
            "description": "Preset name(s) the grant started from. Not authority.",
            "type": "string"
          },
          "sub": {
            "description": "The human who consented.",
            "format": "uuid",
            "type": "string"
          },
          "workspace_id": {
            "format": "uuid",
            "type": "string"
          }
        },
        "required": [
          "active"
        ],
        "type": "object"
      },
      "JwkDto": {
        "properties": {
          "alg": {
            "description": "Signing algorithm (e.g. `RS256`).",
            "example": "RS256",
            "type": "string"
          },
          "kid": {
            "description": "Key ID for matching the kid header on a JWT.",
            "example": "k_2026_05_01",
            "type": "string"
          },
          "kty": {
            "description": "Key type (e.g. `RSA`).",
            "example": "RSA",
            "type": "string"
          },
          "use": {
            "description": "Key usage. Always `sig` for this endpoint.",
            "example": "sig",
            "type": "string"
          }
        },
        "required": [
          "kty",
          "alg",
          "kid",
          "use"
        ],
        "type": "object"
      },
      "JwksResponseDto": {
        "properties": {
          "keys": {
            "description": "Array of public JWKs. Token verifiers iterate these by `kid` to find the matching signer.",
            "items": {
              "$ref": "#/components/schemas/JwkDto"
            },
            "type": "array"
          }
        },
        "required": [
          "keys"
        ],
        "type": "object"
      },
      "ListManagedPoliciesResponseDto": {
        "properties": {
          "data": {
            "items": {
              "$ref": "#/components/schemas/ManagedPolicyResponseDto"
            },
            "type": "array"
          },
          "pagination": {
            "$ref": "#/components/schemas/PaginationDto"
          }
        },
        "required": [
          "data",
          "pagination"
        ],
        "type": "object"
      },
      "ListPlatformApiKeysResponseDto": {
        "properties": {
          "data": {
            "items": {
              "$ref": "#/components/schemas/PublicApiKeyDto"
            },
            "type": "array"
          },
          "pagination": {
            "$ref": "#/components/schemas/PaginationDto"
          }
        },
        "required": [
          "data",
          "pagination"
        ],
        "type": "object"
      },
      "ListWorkspaceAuditLogsResponseDto": {
        "properties": {
          "data": {
            "items": {
              "$ref": "#/components/schemas/WorkspaceAuditLogEntryDto"
            },
            "type": "array"
          },
          "pagination": {
            "$ref": "#/components/schemas/PaginationDto"
          }
        },
        "required": [
          "data",
          "pagination"
        ],
        "type": "object"
      },
      "ListWorkspaceInvitesResponseDto": {
        "properties": {
          "data": {
            "items": {
              "$ref": "#/components/schemas/WorkspaceInviteListItemDto"
            },
            "type": "array"
          },
          "pagination": {
            "$ref": "#/components/schemas/PaginationDto"
          }
        },
        "required": [
          "data",
          "pagination"
        ],
        "type": "object"
      },
      "ListWorkspaceMembersResponseDto": {
        "properties": {
          "data": {
            "items": {
              "$ref": "#/components/schemas/WorkspaceMemberResponseDto"
            },
            "type": "array"
          },
          "pagination": {
            "$ref": "#/components/schemas/PaginationDto"
          }
        },
        "required": [
          "data",
          "pagination"
        ],
        "type": "object"
      },
      "ListWorkspaceRolesResponseDto": {
        "properties": {
          "data": {
            "items": {
              "$ref": "#/components/schemas/WorkspaceRoleResponseDto"
            },
            "type": "array"
          },
          "pagination": {
            "$ref": "#/components/schemas/PaginationDto"
          }
        },
        "required": [
          "data",
          "pagination"
        ],
        "type": "object"
      },
      "ListWorkspaceServicesResponseDto": {
        "properties": {
          "data": {
            "items": {
              "$ref": "#/components/schemas/WorkspaceServiceStateDto"
            },
            "type": "array"
          }
        },
        "required": [
          "data"
        ],
        "type": "object"
      },
      "ListWorkspacesResponseDto": {
        "properties": {
          "data": {
            "items": {
              "$ref": "#/components/schemas/WorkspaceListItemDto"
            },
            "type": "array"
          },
          "pagination": {
            "$ref": "#/components/schemas/PaginationDto"
          }
        },
        "required": [
          "data",
          "pagination"
        ],
        "type": "object"
      },
      "LogoutDto": {
        "properties": {
          "refresh_token": {
            "description": "Refresh token (JWT)",
            "example": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
            "type": "string"
          }
        },
        "required": [
          "refresh_token"
        ],
        "type": "object"
      },
      "ManagedPolicyDetailResponseDto": {
        "properties": {
          "binding_count": {
            "allOf": [
              {
                "$ref": "#/components/schemas/PolicyBindingCountDto"
              }
            ],
            "description": "Counts of currently-bound consumers — surface this in delete-confirm UX so the operator sees cascade impact."
          },
          "created_at": {
            "format": "date-time",
            "type": "string"
          },
          "created_by": {
            "description": "Account that authored the row. Only revealed to callers with `workspace.audit.read`; plain members see null.",
            "format": "uuid",
            "nullable": true,
            "type": "string"
          },
          "description": {
            "nullable": true,
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "name": {
            "example": "mail-readonly",
            "type": "string"
          },
          "policy": {
            "description": "IAM-style policy statements bound to this managed policy.",
            "items": {
              "$ref": "#/components/schemas/PolicyStatementDto"
            },
            "type": "array"
          },
          "updated_at": {
            "format": "date-time",
            "type": "string"
          },
          "workspace_id": {
            "format": "uuid",
            "type": "string"
          }
        },
        "required": [
          "id",
          "workspace_id",
          "name",
          "description",
          "policy",
          "created_by",
          "created_at",
          "updated_at",
          "binding_count"
        ],
        "type": "object"
      },
      "ManagedPolicyResponseDto": {
        "properties": {
          "created_at": {
            "format": "date-time",
            "type": "string"
          },
          "created_by": {
            "description": "Account that authored the row. Only revealed to callers with `workspace.audit.read`; plain members see null.",
            "format": "uuid",
            "nullable": true,
            "type": "string"
          },
          "description": {
            "nullable": true,
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "name": {
            "example": "mail-readonly",
            "type": "string"
          },
          "policy": {
            "description": "IAM-style policy statements bound to this managed policy.",
            "items": {
              "$ref": "#/components/schemas/PolicyStatementDto"
            },
            "type": "array"
          },
          "updated_at": {
            "format": "date-time",
            "type": "string"
          },
          "workspace_id": {
            "format": "uuid",
            "type": "string"
          }
        },
        "required": [
          "id",
          "workspace_id",
          "name",
          "description",
          "policy",
          "created_by",
          "created_at",
          "updated_at"
        ],
        "type": "object"
      },
      "MeProfileDto": {
        "properties": {
          "avatar_url": {
            "description": "Short-lived (15 min) presigned GET URL for the avatar object in private object storage. NULL when no avatar has been set; the frontend then renders initials. The API mints a fresh URL on every response, so a stable bookmark of this value is not meaningful.",
            "nullable": true,
            "type": "string"
          },
          "display_name": {
            "nullable": true,
            "type": "string"
          },
          "email": {
            "nullable": true,
            "type": "string"
          },
          "email_verified_at": {
            "description": "Timestamp when this account verified its primary email, or null if unverified.",
            "format": "date-time",
            "nullable": true,
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "status": {
            "description": "Account status (e.g. `active`, `suspended`).",
            "type": "string"
          },
          "username": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "email",
          "username",
          "display_name",
          "status",
          "email_verified_at",
          "avatar_url"
        ],
        "type": "object"
      },
      "MintPakResponseDto": {
        "properties": {
          "record": {
            "$ref": "#/components/schemas/PublicApiKeyDto"
          },
          "token": {
            "description": "The full plaintext PAK token. Returned ONCE — store it now; it cannot be retrieved later.",
            "example": "pcft_live_abc123XYZ...",
            "type": "string"
          }
        },
        "required": [
          "token",
          "record"
        ],
        "type": "object"
      },
      "OAuthCallbackResultDto": {
        "properties": {
          "return_to": {
            "description": "Original `return_to` the user was sent off with. auth-ui redirects here after writing the cookie.",
            "type": "string"
          },
          "tokens": {
            "allOf": [
              {
                "$ref": "#/components/schemas/TokenResponseDto"
              }
            ],
            "description": "Issued PlatformUser session tokens. auth-ui sets the auth_token cookie from `accessToken` and 302s the browser to `return_to`."
          }
        },
        "required": [
          "tokens",
          "return_to"
        ],
        "type": "object"
      },
      "PaginationDto": {
        "properties": {
          "has_more": {
            "example": false,
            "type": "boolean"
          },
          "next_cursor": {
            "example": null,
            "nullable": true,
            "type": "string"
          }
        },
        "required": [
          "next_cursor",
          "has_more"
        ],
        "type": "object"
      },
      "PolicyBindingCountDto": {
        "properties": {
          "pak": {
            "description": "Number of platform API keys bound to this policy.",
            "type": "number"
          },
          "role": {
            "description": "Number of workspace roles bound to this policy.",
            "type": "number"
          }
        },
        "required": [
          "pak",
          "role"
        ],
        "type": "object"
      },
      "PolicyStatementDto": {
        "properties": {
          "actions": {
            "description": "Action strings the statement applies to. Wildcards (e.g. `social.*`, `*.read`, `*`) supported per `actionMatches` semantics.",
            "example": [
              "social.read",
              "social.list"
            ],
            "items": {
              "type": "string"
            },
            "minItems": 1,
            "type": "array"
          },
          "effect": {
            "description": "Whether the statement allows or denies the listed actions on the listed resources.",
            "enum": [
              "allow",
              "deny"
            ],
            "example": "allow",
            "type": "string"
          },
          "resources": {
            "description": "Resource URNs the statement applies to. Use `[\"*\"]` for workspace-wide. Per-resource scoping (e.g. `pcft:agora:community/<uuid>`) is supported by the PAK lane today; the workspace-role authoring API restricts this to `[\"*\"]` for now (see `tasks/platform-auth-api/001`).",
            "example": [
              "*"
            ],
            "items": {
              "type": "string"
            },
            "minItems": 1,
            "type": "array"
          }
        },
        "required": [
          "effect",
          "actions",
          "resources"
        ],
        "type": "object"
      },
      "PublicApiKeyDto": {
        "properties": {
          "created_at": {
            "format": "date-time",
            "type": "string"
          },
          "created_by": {
            "format": "uuid",
            "nullable": true,
            "type": "string"
          },
          "description": {
            "nullable": true,
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "last_used_at": {
            "format": "date-time",
            "nullable": true,
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "policies": {
            "description": "Managed policies currently bound to this PAK.",
            "items": {
              "$ref": "#/components/schemas/BoundPolicySummaryDto"
            },
            "type": "array"
          },
          "revoked_at": {
            "format": "date-time",
            "nullable": true,
            "type": "string"
          },
          "token_prefix": {
            "description": "The first ~14 chars of the token (prefix). Plaintext token is never returned after mint.",
            "example": "pcft_live_abc123",
            "type": "string"
          },
          "updated_at": {
            "format": "date-time",
            "type": "string"
          },
          "workspace_id": {
            "format": "uuid",
            "type": "string"
          }
        },
        "required": [
          "id",
          "workspace_id",
          "created_by",
          "name",
          "description",
          "token_prefix",
          "policies",
          "last_used_at",
          "revoked_at",
          "created_at",
          "updated_at"
        ],
        "type": "object"
      },
      "RefreshDto": {
        "properties": {
          "refresh_token": {
            "description": "Refresh token (JWT)",
            "example": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
            "type": "string"
          }
        },
        "required": [
          "refresh_token"
        ],
        "type": "object"
      },
      "RequestPasswordResetDto": {
        "properties": {
          "email": {
            "description": "Email address to send reset token",
            "example": "user@example.com",
            "type": "string"
          }
        },
        "required": [
          "email"
        ],
        "type": "object"
      },
      "ResetPasswordDto": {
        "properties": {
          "new_password": {
            "description": "New password (min 8 characters)",
            "example": "MyNewPassword123",
            "type": "string"
          },
          "token": {
            "description": "6-digit reset token sent via email",
            "example": "123456",
            "type": "string"
          }
        },
        "required": [
          "token",
          "new_password"
        ],
        "type": "object"
      },
      "RevocationRequestDto": {
        "properties": {
          "client_id": {
            "type": "string"
          },
          "client_secret": {
            "type": "string"
          },
          "token": {
            "description": "The access or refresh token to revoke.",
            "type": "string"
          },
          "token_type_hint": {
            "description": "`access_token` or `refresh_token`. Advisory only.",
            "type": "string"
          }
        },
        "required": [
          "token"
        ],
        "type": "object"
      },
      "SessionDto": {
        "properties": {
          "account_id": {
            "description": "Owning account identifier",
            "example": "acc_01HZY2F2B6N7M8Q9W4E6R2",
            "type": "string"
          },
          "created_at": {
            "description": "Creation timestamp (ISO 8601)",
            "example": "2025-09-20T12:34:56.000Z",
            "format": "date-time",
            "type": "string"
          },
          "expires_at": {
            "description": "Expiration timestamp (ISO 8601)",
            "example": "2025-09-21T12:34:56.000Z",
            "format": "date-time",
            "type": "string"
          },
          "id": {
            "description": "Session identifier",
            "example": "sess_01HZY2G3A7M8Q9W4E6R2K1",
            "type": "string"
          },
          "ip": {
            "description": "Client IP address",
            "example": "203.0.113.42",
            "nullable": true,
            "type": "string"
          },
          "last_used_at": {
            "description": "Last time this session was used (ISO 8601) or null",
            "example": "2025-09-20T18:00:00.000Z",
            "format": "date-time",
            "nullable": true,
            "type": "string"
          },
          "user_agent": {
            "description": "HTTP User-Agent string",
            "example": "Mozilla/5.0 (Macintosh; Intel Mac OS X 14_5_0) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/125.0.0.0 Safari/537.36",
            "nullable": true,
            "type": "string"
          }
        },
        "required": [
          "id",
          "account_id",
          "ip",
          "user_agent",
          "created_at",
          "expires_at",
          "last_used_at"
        ],
        "type": "object"
      },
      "SetBindingsDto": {
        "properties": {
          "policy_ids": {
            "description": "Replacement set of managed-policy IDs. Empty array is rejected — revoke the PAK to disable it.",
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "required": [
          "policy_ids"
        ],
        "type": "object"
      },
      "SignInDto": {
        "properties": {
          "identifier": {
            "description": "Email or username",
            "example": "user@example.com",
            "type": "string"
          },
          "password": {
            "description": "User password",
            "example": "MySecurePassword123",
            "type": "string"
          },
          "session_duration": {
            "description": "Session duration: \"short\" (24h), \"long\" (90d), or integer seconds (3600–7776000). Defaults to 30 days.",
            "example": "short",
            "type": "object"
          }
        },
        "required": [
          "identifier",
          "password"
        ],
        "type": "object"
      },
      "SignUpDto": {
        "properties": {
          "display_name": {
            "description": "Display name",
            "example": "John Doe",
            "type": "string"
          },
          "email": {
            "description": "User email address",
            "example": "user@example.com",
            "type": "string"
          },
          "password": {
            "description": "User password (min 8 characters)",
            "example": "MySecurePassword123",
            "type": "string"
          },
          "session_duration": {
            "description": "Session duration: \"short\" (24h), \"long\" (90d), or integer seconds (3600–7776000). Defaults to 30 days.",
            "example": "long",
            "type": "object"
          },
          "username": {
            "description": "Unique username (letters, numbers, dot, underscore, hyphen)",
            "example": "john_doe",
            "type": "string"
          }
        },
        "required": [
          "email",
          "username",
          "password"
        ],
        "type": "object"
      },
      "SourceDiagnosticDto": {
        "properties": {
          "count": {
            "type": "number"
          },
          "error": {
            "type": "string"
          },
          "included": {
            "type": "boolean"
          },
          "source": {
            "enum": [
              "workspace_audit",
              "heimdall_audit",
              "agora_moderation"
            ],
            "type": "string"
          }
        },
        "required": [
          "source",
          "included",
          "count"
        ],
        "type": "object"
      },
      "TokenRequestDto": {
        "properties": {
          "client_id": {
            "description": "Client identifier. Required for public clients; confidential clients may instead use HTTP Basic.",
            "type": "string"
          },
          "client_secret": {
            "description": "Client secret for `client_secret_post` authentication.",
            "type": "string"
          },
          "code": {
            "description": "The single-use authorization code. `authorization_code` grant only.",
            "type": "string"
          },
          "code_verifier": {
            "description": "PKCE verifier. Required on the `authorization_code` grant — there is no non-PKCE path.",
            "type": "string"
          },
          "grant_type": {
            "description": "`authorization_code` or `refresh_token`.",
            "example": "authorization_code",
            "type": "string"
          },
          "redirect_uri": {
            "description": "Must be byte-identical to the `redirect_uri` used at `/authorize`. `authorization_code` grant only.",
            "type": "string"
          },
          "refresh_token": {
            "description": "The rotating refresh token. `refresh_token` grant only.",
            "type": "string"
          },
          "resource": {
            "description": "RFC 8707 resource indicator. Must match the one the grant was created for.",
            "type": "string"
          },
          "scope": {
            "description": "Narrowed preset name(s) on the `refresh_token` grant. A preset the original grant did not carry is rejected; narrowing keeps every deny and only removes actions, so it can never widen.",
            "type": "string"
          }
        },
        "required": [
          "grant_type"
        ],
        "type": "object"
      },
      "TokenResponseDto": {
        "properties": {
          "access_token": {
            "description": "Signed JWT, audience-bound to the resource indicator and carrying the workspace, the preset name and the client id. It carries no policy — authority is resolved per request from the grant row and the human’s live policy.",
            "type": "string"
          },
          "expires_in": {
            "description": "Seconds until the access token expires (≤ 900).",
            "example": 900,
            "type": "number"
          },
          "policy": {
            "description": "The policy this token carries. Effective authority is this intersected with the authorizing human's current workspace policy, re-evaluated on every call.",
            "items": {
              "$ref": "#/components/schemas/PolicyStatementDto"
            },
            "type": "array"
          },
          "refresh_token": {
            "description": "Rotating refresh token. The one presented is consumed; replaying it revokes the whole grant family.",
            "type": "string"
          },
          "scope": {
            "description": "Preset name(s) the grant started from. Present for RFC 6749 §5.1 compliance; it is not the authority — `policy` is.",
            "example": "mail",
            "type": "string"
          },
          "token_type": {
            "example": "Bearer",
            "type": "string"
          },
          "workspace_id": {
            "description": "Workspace the grant is scoped to.",
            "format": "uuid",
            "type": "string"
          }
        },
        "required": [
          "access_token",
          "token_type",
          "expires_in",
          "refresh_token",
          "scope",
          "policy",
          "workspace_id"
        ],
        "type": "object"
      },
      "UpdateMemberRoleDto": {
        "properties": {
          "role": {
            "description": "System role shortcut: 'owner' | 'admin' | 'member'. Either `roleId` or `role` must be supplied.",
            "enum": [
              "owner",
              "admin",
              "member"
            ],
            "type": "string"
          },
          "role_id": {
            "description": "UUID of the role to assign. Use this to target custom roles.",
            "example": "11111111-1111-4111-8111-111111111111",
            "type": "string"
          }
        },
        "type": "object"
      },
      "UpdateMemberRoleResponseDto": {
        "properties": {
          "account_id": {
            "format": "uuid",
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "invited_by": {
            "format": "uuid",
            "nullable": true,
            "type": "string"
          },
          "joined_at": {
            "format": "date-time",
            "type": "string"
          },
          "role_id": {
            "format": "uuid",
            "type": "string"
          },
          "workspace_id": {
            "format": "uuid",
            "type": "string"
          }
        },
        "required": [
          "id",
          "workspace_id",
          "account_id",
          "role_id",
          "invited_by",
          "joined_at"
        ],
        "type": "object"
      },
      "UpdatePakDto": {
        "properties": {
          "description": {
            "maxLength": 500,
            "nullable": true,
            "type": "string"
          },
          "name": {
            "maxLength": 120,
            "minLength": 1,
            "type": "string"
          }
        },
        "type": "object"
      },
      "UpdateProfileDto": {
        "properties": {
          "display_name": {
            "description": "New display name. 1-128 chars. Optional — empty PATCH is a no-op.",
            "maxLength": 128,
            "minLength": 1,
            "type": "string"
          }
        },
        "type": "object"
      },
      "UpdateWorkspaceDto": {
        "properties": {
          "display_name": {
            "maxLength": 128,
            "minLength": 1,
            "type": "string"
          },
          "settings": {
            "description": "Free-form workspace settings blob.",
            "type": "object"
          },
          "slug": {
            "description": "New URL-friendly slug. Same constraints as the create form. Renames are broadcast on the workspace events queue so denormalised consumers (waitlist-api, future product surfaces) can sync. Old URLs continue to resolve for one rename cycle.",
            "example": "acme",
            "maxLength": 64,
            "minLength": 2,
            "type": "string"
          }
        },
        "type": "object"
      },
      "UpdateWorkspacePolicyDto": {
        "properties": {
          "description": {
            "description": "Optional human description. Pass `null` to clear an existing description on PATCH.",
            "example": "Read-only access to all Mail resources",
            "nullable": true,
            "type": "string"
          },
          "name": {
            "description": "Policy name (1-64 chars, alphanumeric + space/underscore/hyphen). Unique per workspace.",
            "example": "mail-readonly",
            "maxLength": 64,
            "minLength": 1,
            "type": "string"
          },
          "policy": {
            "description": "IAM-style policy. Empty array means \"no permissions\" — useful as a placeholder before granting any actions.",
            "example": [
              {
                "actions": [
                  "mail.read",
                  "mail.list"
                ],
                "effect": "allow",
                "resources": [
                  "*"
                ]
              }
            ],
            "items": {
              "$ref": "#/components/schemas/PolicyStatementDto"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "UpdateWorkspaceRoleDto": {
        "properties": {
          "description": {
            "maxLength": 256,
            "type": "string"
          },
          "name": {
            "maxLength": 64,
            "minLength": 1,
            "type": "string"
          },
          "policy": {
            "description": "Replaces the role policy if provided. Omit to leave policy unchanged.",
            "items": {
              "$ref": "#/components/schemas/PolicyStatementDto"
            },
            "type": "array"
          },
          "policy_id": {
            "description": "Bind / unbind the role to a managed policy. UUID → bind, null → unbind, omit → leave unchanged.",
            "nullable": true,
            "type": "string"
          }
        },
        "type": "object"
      },
      "VerifyCodeDto": {
        "properties": {
          "code": {
            "description": "Verification code from the email link.",
            "example": "a1b2c3d4…",
            "type": "string"
          }
        },
        "required": [
          "code"
        ],
        "type": "object"
      },
      "WorkspaceActionCatalogResponseDto": {
        "properties": {
          "actions": {
            "description": "Every known workspace action across all enabled service catalogs.",
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "required": [
          "actions"
        ],
        "type": "object"
      },
      "WorkspaceAuditLogEntryDto": {
        "properties": {
          "action": {
            "description": "Action identifier (e.g. `workspace.member.removed`).",
            "example": "workspace.member.removed",
            "type": "string"
          },
          "actor_id": {
            "description": "Account UUID of the actor, or null for system actions.",
            "nullable": true,
            "type": "string"
          },
          "actor_type": {
            "description": "Actor classification.",
            "enum": [
              "platform_user",
              "system",
              "pat"
            ],
            "example": "platform_user",
            "type": "string"
          },
          "created_at": {
            "format": "date-time",
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "ip": {
            "description": "Originating request IP, when available.",
            "nullable": true,
            "type": "string"
          },
          "metadata": {
            "description": "Free-form metadata attached to the audit row (jsonb).",
            "type": "object"
          },
          "resource": {
            "description": "Resource type the action targeted.",
            "example": "workspace_membership",
            "type": "string"
          },
          "resource_id": {
            "format": "uuid",
            "nullable": true,
            "type": "string"
          },
          "workspace_id": {
            "format": "uuid",
            "type": "string"
          }
        },
        "required": [
          "id",
          "workspace_id",
          "actor_id",
          "actor_type",
          "action",
          "resource",
          "resource_id",
          "ip",
          "metadata",
          "created_at"
        ],
        "type": "object"
      },
      "WorkspaceAvatarUrlResponseDto": {
        "properties": {
          "avatar_url": {
            "type": "string"
          }
        },
        "required": [
          "avatar_url"
        ],
        "type": "object"
      },
      "WorkspaceInviteListItemDto": {
        "properties": {
          "code": {
            "description": "Single-use invite code embedded in the invite URL.",
            "type": "string"
          },
          "created_at": {
            "format": "date-time",
            "type": "string"
          },
          "created_by": {
            "format": "uuid",
            "type": "string"
          },
          "email": {
            "format": "email",
            "nullable": true,
            "type": "string"
          },
          "expires_at": {
            "format": "date-time",
            "nullable": true,
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "max_uses": {
            "nullable": true,
            "type": "number"
          },
          "revoked_at": {
            "format": "date-time",
            "nullable": true,
            "type": "string"
          },
          "role_id": {
            "format": "uuid",
            "nullable": true,
            "type": "string"
          },
          "role_name": {
            "nullable": true,
            "type": "string"
          },
          "use_count": {
            "type": "number"
          },
          "workspace_id": {
            "format": "uuid",
            "type": "string"
          }
        },
        "required": [
          "id",
          "workspace_id",
          "code",
          "email",
          "role_id",
          "max_uses",
          "use_count",
          "expires_at",
          "revoked_at",
          "created_at",
          "created_by",
          "role_name"
        ],
        "type": "object"
      },
      "WorkspaceInviteResponseDto": {
        "properties": {
          "code": {
            "description": "Single-use invite code embedded in the invite URL.",
            "type": "string"
          },
          "created_at": {
            "format": "date-time",
            "type": "string"
          },
          "created_by": {
            "format": "uuid",
            "type": "string"
          },
          "email": {
            "format": "email",
            "nullable": true,
            "type": "string"
          },
          "expires_at": {
            "format": "date-time",
            "nullable": true,
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "max_uses": {
            "nullable": true,
            "type": "number"
          },
          "revoked_at": {
            "format": "date-time",
            "nullable": true,
            "type": "string"
          },
          "role_id": {
            "format": "uuid",
            "nullable": true,
            "type": "string"
          },
          "use_count": {
            "type": "number"
          },
          "workspace_id": {
            "format": "uuid",
            "type": "string"
          }
        },
        "required": [
          "id",
          "workspace_id",
          "code",
          "email",
          "role_id",
          "max_uses",
          "use_count",
          "expires_at",
          "revoked_at",
          "created_at",
          "created_by"
        ],
        "type": "object"
      },
      "WorkspaceListItemDto": {
        "properties": {
          "created_at": {
            "format": "date-time",
            "type": "string"
          },
          "created_by": {
            "format": "uuid",
            "type": "string"
          },
          "display_name": {
            "example": "Acme Corp",
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "member_role": {
            "description": "Caller’s role name in this workspace.",
            "example": "owner",
            "nullable": true,
            "type": "string"
          },
          "member_role_id": {
            "description": "Caller’s workspace_role.id for this workspace.",
            "format": "uuid",
            "nullable": true,
            "type": "string"
          },
          "settings": {
            "description": "Free-form workspace settings blob.",
            "type": "object"
          },
          "slug": {
            "example": "acme-corp",
            "type": "string"
          },
          "status": {
            "description": "Workspace status (e.g. `active`).",
            "example": "active",
            "type": "string"
          },
          "updated_at": {
            "format": "date-time",
            "type": "string"
          }
        },
        "required": [
          "id",
          "slug",
          "display_name",
          "created_by",
          "status",
          "settings",
          "created_at",
          "updated_at",
          "member_role_id",
          "member_role"
        ],
        "type": "object"
      },
      "WorkspaceMemberResponseDto": {
        "properties": {
          "account_id": {
            "format": "uuid",
            "type": "string"
          },
          "avatar_url": {
            "description": "Presigned URL for the member's uploaded avatar, or null when they have none. Short-lived — mint a fresh one by re-reading this endpoint rather than caching the URL.",
            "nullable": true,
            "type": "string"
          },
          "display_name": {
            "nullable": true,
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "joined_at": {
            "format": "date-time",
            "type": "string"
          },
          "primary_email": {
            "nullable": true,
            "type": "string"
          },
          "role": {
            "type": "string"
          },
          "role_id": {
            "format": "uuid",
            "type": "string"
          },
          "role_is_system": {
            "type": "boolean"
          },
          "username": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "account_id",
          "username",
          "display_name",
          "primary_email",
          "avatar_url",
          "role_id",
          "role",
          "role_is_system",
          "joined_at"
        ],
        "type": "object"
      },
      "WorkspaceResponseDto": {
        "properties": {
          "created_at": {
            "format": "date-time",
            "type": "string"
          },
          "created_by": {
            "format": "uuid",
            "type": "string"
          },
          "display_name": {
            "example": "Acme Corp",
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "settings": {
            "description": "Free-form workspace settings blob.",
            "type": "object"
          },
          "slug": {
            "example": "acme-corp",
            "type": "string"
          },
          "status": {
            "description": "Workspace status (e.g. `active`).",
            "example": "active",
            "type": "string"
          },
          "updated_at": {
            "format": "date-time",
            "type": "string"
          }
        },
        "required": [
          "id",
          "slug",
          "display_name",
          "created_by",
          "status",
          "settings",
          "created_at",
          "updated_at"
        ],
        "type": "object"
      },
      "WorkspaceRoleResponseDto": {
        "properties": {
          "description": {
            "nullable": true,
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "is_system": {
            "description": "True for built-in system roles (owner/admin/member); false for custom roles.",
            "type": "boolean"
          },
          "name": {
            "description": "Role name. System role names are `owner`/`admin`/`member`.",
            "type": "string"
          },
          "policy": {
            "description": "Effective IAM-style policy. When `policy_id` is set this is the bound managed policy; otherwise it is the inline policy column.",
            "items": {
              "$ref": "#/components/schemas/PolicyStatementDto"
            },
            "type": "array"
          },
          "policy_id": {
            "description": "Managed policy ID if the role is bound to one, else null.",
            "format": "uuid",
            "nullable": true,
            "type": "string"
          }
        },
        "required": [
          "id",
          "name",
          "description",
          "is_system",
          "policy",
          "policy_id"
        ],
        "type": "object"
      },
      "WorkspaceServiceStateDto": {
        "properties": {
          "disabled_at": {
            "format": "date-time",
            "nullable": true,
            "type": "string"
          },
          "disabled_by": {
            "format": "uuid",
            "nullable": true,
            "type": "string"
          },
          "enabled": {
            "type": "boolean"
          },
          "enabled_at": {
            "format": "date-time",
            "nullable": true,
            "type": "string"
          },
          "enabled_by": {
            "format": "uuid",
            "nullable": true,
            "type": "string"
          },
          "service": {
            "description": "Service identifier (e.g. `mail`, `waitlist`, `social`, `auth`).",
            "example": "mail",
            "type": "string"
          },
          "settings": {
            "description": "Per-service settings blob (service-specific shape).",
            "type": "object"
          },
          "workspace_id": {
            "format": "uuid",
            "type": "string"
          }
        },
        "required": [
          "workspace_id",
          "service",
          "enabled",
          "enabled_at",
          "enabled_by",
          "disabled_at",
          "disabled_by",
          "settings"
        ],
        "type": "object"
      }
    },
    "securitySchemes": {
      "bearer": {
        "bearerFormat": "JWT",
        "scheme": "bearer",
        "type": "http"
      }
    }
  },
  "info": {
    "contact": {},
    "description": "PlatformUser authentication and workspace administration for ProductCraft. Owns sign-in, sign-up, password reset, workspaces, members, invites, workspace roles, workspace-service toggles, and the introspect endpoint every other backend uses to resolve permissions.",
    "title": "Platform Auth API",
    "version": "1.0.0"
  },
  "openapi": "3.0.0",
  "paths": {
    "/.well-known/jwks.json": {
      "get": {
        "operationId": "JwksController_jwks",
        "parameters": [],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JwksResponseDto"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Public JWKS for verifying platform-issued JWTs (issuer `https://api.auth.productcraft.co`).",
        "tags": [
          "jwks"
        ]
      }
    },
    "/.well-known/oauth-authorization-server": {
      "get": {
        "description": "Discovery document for third-party clients (Claude Desktop, Cursor, the ProductCraft MCP server). PKCE with `S256` is mandatory; no other code-challenge method is advertised or accepted. `scopes_supported` lists **presets**, not scopes: authority is the policy document on the grant (`authority_model`).",
        "operationId": "AuthorizationServerMetadataController_metadata",
        "parameters": [],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AuthorizationServerMetadataDto"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "OAuth 2.1 authorization server metadata (RFC 8414)",
        "tags": [
          "oauth-authorization-server"
        ]
      }
    },
    "/v1/account": {
      "delete": {
        "operationId": "AccountController_deleteAccount",
        "parameters": [],
        "responses": {
          "204": {
            "description": "Account deleted. All sessions revoked, and any linked Sign in with Apple grant is queued for revocation with Apple. The revocation is durable and retried in the background, so a 204 does not depend on Apple being reachable."
          },
          "403": {
            "description": "The session lacks the required action for self-account deletion (defence-in-depth — the route is guarded but the cookie may be present without action access)."
          },
          "404": {
            "description": "No account row exists for the session principal. Should never fire for a valid session — surfaced for SDKs."
          }
        },
        "summary": "Delete the calling account. Auto-promotes the oldest admin (or oldest non-self member) to owner in any workspace where the departing account is the sole owner. Linked provider grants that require server-side revocation (Sign in with Apple) are revoked with the provider.",
        "tags": [
          "account"
        ]
      }
    },
    "/v1/auth/logout": {
      "post": {
        "operationId": "AuthenticationController_logout",
        "parameters": [],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LogoutDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Logout successful (no content)"
          },
          "400": {
            "description": "Validation failed or invalid token"
          }
        },
        "summary": "Logout by revoking refresh token",
        "tags": [
          "authentication"
        ]
      }
    },
    "/v1/auth/logout-current": {
      "post": {
        "operationId": "AuthenticationController_logoutCurrent",
        "parameters": [],
        "responses": {
          "204": {
            "description": "Logout successful (no content)"
          },
          "401": {
            "description": "Missing or invalid access token"
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Revoke the server-side session tied to the caller’s current access token",
        "tags": [
          "authentication"
        ]
      }
    },
    "/v1/auth/oauth/{provider}/callback": {
      "post": {
        "operationId": "OAuthController_callback",
        "parameters": [
          {
            "in": "path",
            "name": "provider",
            "required": true,
            "schema": {
              "enum": [
                "apple",
                "google",
                "github"
              ],
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CallbackBodyDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OAuthCallbackResultDto"
                }
              }
            },
            "description": ""
          },
          "400": {
            "description": "Unknown / expired state, provider state mismatch, or unknown provider id."
          },
          "401": {
            "description": "Provider token verification failed, nonce replayed, or downstream identity could not be resolved."
          }
        },
        "summary": "Complete a federated sign-in. auth-ui proxies the provider redirect (query params for Google/GitHub, form_post body for Apple) into this endpoint and uses the returned `tokens` to set the auth_token cookie.",
        "tags": [
          "OAuth Sign-In"
        ]
      }
    },
    "/v1/auth/oauth/{provider}/start": {
      "get": {
        "operationId": "OAuthController_start",
        "parameters": [
          {
            "description": "OAuth provider id. Providers are gated on env-var configuration; unconfigured providers 400 with UNKNOWN_OAUTH_PROVIDER.",
            "in": "path",
            "name": "provider",
            "required": true,
            "schema": {
              "enum": [
                "apple",
                "google",
                "github"
              ],
              "type": "string"
            }
          },
          {
            "description": "Absolute URL the user lands on after a successful signin. Must match the `OAUTH_RETURN_TO_ORIGINS` allowlist (CSV, wildcards supported as `https://*.productcraft.co`).",
            "in": "query",
            "name": "return_to",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "302": {
            "description": "Redirect to provider authorize URL."
          },
          "400": {
            "description": "Invalid `return_to` or unknown provider."
          }
        },
        "summary": "Begin a federated sign-in flow; 302s to the provider authorize URL.",
        "tags": [
          "OAuth Sign-In"
        ]
      }
    },
    "/v1/auth/password/request-reset": {
      "post": {
        "operationId": "PasswordController_requestPasswordReset",
        "parameters": [],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RequestPasswordResetDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Reset request accepted. Identical whether or not the email matched an account — if it did, an email has been queued."
          }
        },
        "summary": "Request a password reset email. The response shape never reveals whether the email matched an account.",
        "tags": [
          "authentication"
        ]
      }
    },
    "/v1/auth/password/reset": {
      "post": {
        "operationId": "PasswordController_resetPassword",
        "parameters": [],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ResetPasswordDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Password reset; all active sessions revoked."
          }
        },
        "summary": "Reset password using the email-delivered reset token.",
        "tags": [
          "authentication"
        ]
      }
    },
    "/v1/auth/refresh": {
      "post": {
        "operationId": "AuthenticationController_refresh",
        "parameters": [],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RefreshDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TokenResponseDto"
                }
              }
            },
            "description": "Token response"
          },
          "400": {
            "description": "Validation failed or invalid token"
          }
        },
        "summary": "Refresh access token using refresh token",
        "tags": [
          "authentication"
        ]
      }
    },
    "/v1/auth/signin": {
      "post": {
        "operationId": "AuthenticationController_signin",
        "parameters": [],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SignInDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TokenResponseDto"
                }
              }
            },
            "description": "Token response"
          },
          "400": {
            "description": "Validation failed"
          }
        },
        "summary": "Sign in with email/username and password",
        "tags": [
          "authentication"
        ]
      }
    },
    "/v1/auth/signup": {
      "post": {
        "operationId": "AuthenticationController_signup",
        "parameters": [],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SignUpDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TokenResponseDto"
                }
              }
            },
            "description": "Token response"
          },
          "400": {
            "description": "Validation failed"
          }
        },
        "summary": "Create a new account and sign in",
        "tags": [
          "authentication"
        ]
      }
    },
    "/v1/introspect": {
      "get": {
        "operationId": "IntrospectController_introspect",
        "parameters": [],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/IntrospectResponseDto"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Introspect current user with workspace list",
        "tags": [
          "introspect"
        ]
      }
    },
    "/v1/introspect/api-key": {
      "post": {
        "operationId": "IntrospectController_introspectApiKey",
        "parameters": [
          {
            "in": "header",
            "name": "authorization",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/IntrospectApiKeyResponseDto"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Resolve a Platform API Key (PAK) to its workspace + IAM-style policy. Downstream services use this with a 60s LRU cache and evaluate the policy via @repo/authz-nestjs evaluatePolicy().",
        "tags": [
          "introspect"
        ]
      }
    },
    "/v1/introspect/workspaces/{workspace_id}": {
      "get": {
        "operationId": "IntrospectController_introspectWorkspace",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/IntrospectWorkspaceResponseDto"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Introspect caller's membership + effective policy + enabled services in a workspace",
        "tags": [
          "introspect"
        ]
      }
    },
    "/v1/me": {
      "get": {
        "operationId": "MeController_getMe",
        "parameters": [],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MeProfileDto"
                }
              }
            },
            "description": ""
          },
          "401": {
            "description": "No session."
          },
          "404": {
            "description": "No account row for the session principal. Should never fire for a valid session — surfaced for SDKs."
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Read the caller's account profile.",
        "tags": [
          "me"
        ]
      }
    },
    "/v1/me/avatar": {
      "delete": {
        "operationId": "MeController_removeAvatar",
        "parameters": [],
        "responses": {
          "204": {
            "description": "Avatar removed (or already absent)."
          },
          "401": {
            "description": "No session."
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Clear the caller's avatar and delete the stored object.",
        "tags": [
          "me"
        ]
      },
      "post": {
        "operationId": "MeController_setAvatar",
        "parameters": [],
        "requestBody": {
          "content": {
            "multipart/form-data": {
              "schema": {
                "properties": {
                  "file": {
                    "description": "PNG, JPEG, or WEBP image, ≤2 MB.",
                    "format": "binary",
                    "type": "string"
                  }
                },
                "required": [
                  "file"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AvatarUrlResponseDto"
                }
              }
            },
            "description": ""
          },
          "400": {
            "description": "Invalid file: wrong mimetype, oversized, or content does not match the declared type."
          },
          "401": {
            "description": "No session."
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Upload (or replace) the caller's avatar. PNG/JPEG/WEBP only — SVG is rejected because user-uploaded SVG from a productcraft.co origin is a stored-XSS vector. ≤2 MB. Magic bytes are sniffed; a mislabeled file is rejected.",
        "tags": [
          "me"
        ]
      }
    },
    "/v1/me/profile": {
      "patch": {
        "operationId": "MeController_updateProfile",
        "parameters": [],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateProfileDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MeProfileDto"
                }
              }
            },
            "description": ""
          },
          "400": {
            "description": "Validation failed (e.g. displayName outside 1-128 chars)."
          },
          "401": {
            "description": "No session."
          },
          "403": {
            "description": "The caller authenticated with a non-human principal (e.g. PAK). Profile is per-human; only the human owner can mutate their own row."
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Update the caller's profile. Currently only `displayName` is editable. An empty body is a no-op that returns the current profile. Cookie/JWT only — this is a human-self-service surface, not a PAK-mutation surface.",
        "tags": [
          "me"
        ]
      }
    },
    "/v1/oauth/authorize": {
      "get": {
        "description": "Requires PKCE with `code_challenge_method=S256`; `plain` and a missing challenge are both rejected. `redirect_uri` must match a registered URI exactly, with only the RFC 8252 §7.3 loopback port relaxation. Responds 302 either to sign-in (no session) or to the consent screen.",
        "operationId": "OAuthAuthorizeController_authorize",
        "parameters": [
          {
            "description": "RFC 8707 resource indicator. Becomes the access token `aud`.",
            "in": "query",
            "name": "resource",
            "required": false,
            "schema": {}
          },
          {
            "in": "query",
            "name": "state",
            "required": false,
            "schema": {}
          },
          {
            "in": "query",
            "name": "code_challenge_method",
            "required": true,
            "schema": {
              "example": "S256"
            }
          },
          {
            "in": "query",
            "name": "code_challenge",
            "required": true,
            "schema": {}
          },
          {
            "description": "Space-delimited preset names (`readonly`, `mail`, `support`, `full`). A preset resolves to a policy template the human narrows on the consent screen; it is never authority in itself.",
            "in": "query",
            "name": "scope",
            "required": true,
            "schema": {
              "example": "mail"
            }
          },
          {
            "in": "query",
            "name": "redirect_uri",
            "required": true,
            "schema": {}
          },
          {
            "in": "query",
            "name": "client_id",
            "required": true,
            "schema": {}
          },
          {
            "in": "query",
            "name": "response_type",
            "required": true,
            "schema": {
              "example": "code"
            }
          }
        ],
        "responses": {
          "302": {
            "description": "Redirect to sign-in or to the consent screen."
          },
          "400": {
            "description": "Rendered `{ error, error_description }` when the failure is in `client_id` or `redirect_uri` — those are never bounced to an unvalidated URI."
          }
        },
        "summary": "OAuth 2.1 authorization endpoint",
        "tags": [
          "oauth-authorization-server"
        ]
      }
    },
    "/v1/oauth/connections": {
      "get": {
        "description": "The user-facing half of revocation: which third-party clients hold a grant, in which workspace, carrying which policy.",
        "operationId": "OAuthAuthorizeController_connections",
        "parameters": [
          {
            "description": "Page size. Defaults to 20, clamped to 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Opaque `next_cursor` from the previous page.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ConnectedClientListDto"
                }
              }
            },
            "description": "Page of connections, newest first, with `next_cursor` and `has_more`."
          },
          "400": {
            "description": "Malformed `cursor`."
          }
        },
        "summary": "List clients this human has connected",
        "tags": [
          "oauth-authorization-server"
        ]
      }
    },
    "/v1/oauth/connections/{grant_id}": {
      "delete": {
        "description": "Revokes the whole grant family — the grant, its authorization codes, its refresh tokens and every live access token — in one transaction.",
        "operationId": "OAuthAuthorizeController_disconnect",
        "parameters": [
          {
            "in": "path",
            "name": "grant_id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Disconnected."
          },
          "404": {
            "description": "No such connection for this human."
          }
        },
        "summary": "Disconnect a client",
        "tags": [
          "oauth-authorization-server"
        ]
      }
    },
    "/v1/oauth/consent/{request_id}": {
      "get": {
        "description": "Backs the consent screen (task 002). Readable only by the signed-in human the request was created for; anyone else gets the same 404 a nonexistent request gets.",
        "operationId": "OAuthAuthorizeController_consentRequest",
        "parameters": [
          {
            "in": "path",
            "name": "request_id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ConsentRequestDto"
                }
              }
            },
            "description": ""
          },
          "404": {
            "description": "No such pending request for this human."
          },
          "409": {
            "description": "Request already used."
          },
          "410": {
            "description": "Request expired."
          }
        },
        "summary": "Read a pending consent request",
        "tags": [
          "oauth-authorization-server"
        ]
      },
      "post": {
        "description": "Approving mints a single-use authorization code bound to the client, the redirect URI, the PKCE challenge, the chosen workspace and the approved policy. The policy is narrowed against the human's own authority through the same `assertCallerCanGrant` the workspace-role and PAK lanes use; a grant that exceeds the granter is refused, never trimmed. Denying returns a redirect carrying `error=access_denied`.\n\nCSRF: the session must be presented as an `Authorization: Bearer` header. The ambient `auth_token` cookie is not accepted on its own, and a request whose `Sec-Fetch-Site` / `Origin` says a browser on another origin is driving it is refused — cookie policy cannot be the only CSRF control on the endpoint that mints a code.",
        "operationId": "OAuthAuthorizeController_decide",
        "parameters": [
          {
            "in": "path",
            "name": "request_id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ConsentDecisionDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ConsentDecisionResponseDto"
                }
              }
            },
            "description": ""
          },
          "400": {
            "description": "Malformed policy, empty policy, or too many statements."
          },
          "403": {
            "description": "Refused before authentication with `CROSS_ORIGIN_REJECTED` when the request carries no bearer or a browser on another origin is driving it. Otherwise: not a member of the chosen workspace, or the approved policy exceeds the human’s own authority — `details.refused_statements` names which statements and why. Also `ACCOUNT_SUSPENDED` / `ACCOUNT_DEACTIVATED` when the account is no longer active, and `OAUTH_GRANT_ADMIN_REVOKED` when a workspace administrator revoked this connection and has not lifted it."
          },
          "404": {
            "description": "No such pending request for this human."
          },
          "409": {
            "description": "Request already used."
          }
        },
        "summary": "Record the human’s consent decision",
        "tags": [
          "oauth-authorization-server"
        ]
      }
    },
    "/v1/oauth/delegated/authorize": {
      "post": {
        "description": "Returns the live effective-policy decision for a single `(action, resource)` pair. Evaluated per call against the grant’s policy AND the authorizing human’s current workspace policy — never from a decision cached at consent time. The delegated access token is presented as the bearer.",
        "operationId": "OAuthDelegatedAuthorizationController_authorize",
        "parameters": [
          {
            "in": "header",
            "name": "authorization",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DelegatedAuthorizationRequestDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DelegatedAuthorizationResponseDto"
                }
              }
            },
            "description": ""
          },
          "401": {
            "description": "Missing or malformed `Authorization` header."
          }
        },
        "summary": "Decide one action for a delegated access token",
        "tags": [
          "oauth-authorization-server"
        ]
      }
    },
    "/v1/oauth/delegated/authorize-assertion": {
      "post": {
        "description": "The REST-service counterpart of `POST /authorize`. A service holds only the short-lived assertion, never the delegated access token, so it asks here. Evaluated against the grant’s consented policy AND the authorizing human’s current workspace policy, live and uncached — a revoked grant, a demoted human or an offboarded one is refused on the next call.",
        "operationId": "OAuthDelegatedAuthorizationController_authorizeAssertion",
        "parameters": [
          {
            "in": "header",
            "name": "authorization",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AssertionAuthorizationRequestDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DelegatedAuthorizationResponseDto"
                }
              }
            },
            "description": ""
          },
          "401": {
            "description": "Missing or malformed `Authorization` header."
          }
        },
        "summary": "Decide one action for a downstream assertion",
        "tags": [
          "oauth-authorization-server"
        ]
      }
    },
    "/v1/oauth/delegated/authorize-batch": {
      "post": {
        "description": "Evaluates a list of actions against the same live effective policy as `POST /authorize`, in one pass. Exists so an MCP server can filter a several-hundred-tool catalog down to what the session actually permits without a round trip per tool. It is a **listing** aid: a tool invocation is still decided on its own, per call.",
        "operationId": "OAuthDelegatedAuthorizationController_authorizeBatch",
        "parameters": [
          {
            "in": "header",
            "name": "authorization",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DelegatedAuthorizationBatchRequestDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DelegatedAuthorizationBatchResponseDto"
                }
              }
            },
            "description": ""
          },
          "400": {
            "description": "Empty or oversized `actions` list."
          },
          "401": {
            "description": "Missing or malformed `Authorization` header."
          }
        },
        "summary": "Decide many actions for a delegated access token",
        "tags": [
          "oauth-authorization-server"
        ]
      }
    },
    "/v1/oauth/delegated/downstream-assertion": {
      "post": {
        "description": "Exchanges a delegated access token for a ~60s assertion audienced at ONE named service, so that service can verify the caller offline without calling back (`docs/mcp.md` §5b). The assertion carries identity — account, workspace, client, grant — and deliberately no policy: the target resolves the delegated effective policy per request through `/v1/introspect`, so a revoked grant or a demoted human is refused on the next call rather than at this assertion’s expiry.",
        "operationId": "OAuthDelegatedAuthorizationController_downstreamAssertion",
        "parameters": [
          {
            "in": "header",
            "name": "authorization",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DownstreamAssertionRequestDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DownstreamAssertionResponseDto"
                }
              }
            },
            "description": ""
          },
          "400": {
            "description": "The requested `audience` is not a service we issue assertions for."
          },
          "401": {
            "description": "Missing or malformed `Authorization` header, or the token/grant behind it is no longer live."
          }
        },
        "summary": "Mint a short-lived assertion for one downstream service",
        "tags": [
          "oauth-authorization-server"
        ]
      }
    },
    "/v1/oauth/introspect": {
      "post": {
        "description": "Client-authenticated, and a client may only introspect its own tokens. `policy` is the ceiling the human approved; `effective_policy` is that intersected with the human's current workspace policy, resolved live on every call with no cache, so a demotion or an offboarding lands on the next call rather than at token expiry.",
        "operationId": "OAuthTokenController_introspect",
        "parameters": [],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/IntrospectionRequestDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/IntrospectionResponseDto"
                }
              }
            },
            "description": ""
          },
          "401": {
            "description": "`invalid_client` — client authentication failed."
          }
        },
        "summary": "Introspect an access token (RFC 7662)",
        "tags": [
          "oauth-authorization-server"
        ]
      }
    },
    "/v1/oauth/register": {
      "post": {
        "description": "Public endpoint, rate-limited per source IP. Every client registered here is marked **unverified** and stays that way; the consent screen shows the badge. `client_secret` and `registration_access_token` are returned once and stored only as hashes.",
        "operationId": "OAuthRegistrationController_register",
        "parameters": [],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ClientRegistrationRequestDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ClientRegistrationResponseDto"
                }
              }
            },
            "description": ""
          },
          "400": {
            "description": "`invalid_redirect_uri` or `invalid_client_metadata` (RFC 7591 §3.2.2)."
          },
          "429": {
            "description": "Too many registrations from this IP."
          },
          "503": {
            "description": "`temporarily_unavailable` — the rate-limit backend is unreachable, so registration fails closed."
          }
        },
        "summary": "Register an OAuth client dynamically (RFC 7591)",
        "tags": [
          "oauth-authorization-server"
        ]
      }
    },
    "/v1/oauth/register/{client_id}": {
      "get": {
        "description": "Requires `Authorization: Bearer <registration_access_token>`. The token addresses exactly one registration; presenting it for a different `client_id` fails.",
        "operationId": "OAuthRegistrationController_read",
        "parameters": [
          {
            "description": "The `client_id` to read.",
            "in": "path",
            "name": "client_id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ClientMetadataDto"
                }
              }
            },
            "description": ""
          },
          "401": {
            "description": "`invalid_client` — bad registration access token."
          }
        },
        "summary": "Read a client registration (RFC 7592)",
        "tags": [
          "oauth-authorization-server"
        ]
      },
      "put": {
        "description": "Requires `Authorization: Bearer <registration_access_token>`. Only display metadata, redirect URIs and the scope ceiling can change — a client cannot promote itself to verified, nor change its authentication method.",
        "operationId": "OAuthRegistrationController_update",
        "parameters": [
          {
            "description": "The `client_id` to update.",
            "in": "path",
            "name": "client_id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ClientRegistrationRequestDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ClientMetadataDto"
                }
              }
            },
            "description": ""
          },
          "401": {
            "description": "`invalid_client` — bad registration access token."
          }
        },
        "summary": "Update a client registration (RFC 7592)",
        "tags": [
          "oauth-authorization-server"
        ]
      }
    },
    "/v1/oauth/revoke": {
      "post": {
        "description": "Revoking a refresh token takes the whole grant family with it. Always answers 200, including for an unknown token — otherwise the endpoint would be a token-validity oracle.",
        "operationId": "OAuthTokenController_revoke",
        "parameters": [],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RevocationRequestDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Revoked, or the token was already unknown."
          },
          "401": {
            "description": "`invalid_client` — client authentication failed."
          }
        },
        "summary": "Revoke a token (RFC 7009)",
        "tags": [
          "oauth-authorization-server"
        ]
      }
    },
    "/v1/oauth/token": {
      "post": {
        "description": "Accepts `authorization_code` (with a mandatory `code_verifier`) and `refresh_token`. Both credentials are single-use: replaying a code or a refresh token issues nothing and revokes the entire grant family, so anything already minted from that grant stops working immediately.",
        "operationId": "OAuthTokenController_token",
        "parameters": [],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TokenRequestDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TokenResponseDto"
                }
              }
            },
            "description": ""
          },
          "400": {
            "description": "`invalid_request`, `invalid_grant`, `invalid_scope`, `invalid_target`, or `unsupported_grant_type`."
          },
          "401": {
            "description": "`invalid_client` — client authentication failed."
          }
        },
        "summary": "OAuth 2.1 token endpoint",
        "tags": [
          "oauth-authorization-server"
        ]
      }
    },
    "/v1/oauth/workspaces/{workspace_id}/connections": {
      "get": {
        "description": "The workspace-side half of revocation, for owners and admins: every grant made against this workspace, by any member, including grants made by people who have since left. Requires `workspace.connection.read`.",
        "operationId": "OAuthAuthorizeController_workspaceConnections",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          },
          {
            "description": "Page size. Defaults to 20, clamped to 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Opaque `next_cursor` from the previous page.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ConnectedClientListDto"
                }
              }
            },
            "description": "Page of connections, newest first, with `next_cursor` and `has_more`."
          },
          "400": {
            "description": "Malformed `cursor`."
          },
          "403": {
            "description": "Not a member, or lacks `workspace.connection.read`."
          }
        },
        "summary": "List every client connected to a workspace",
        "tags": [
          "oauth-authorization-server"
        ]
      }
    },
    "/v1/oauth/workspaces/{workspace_id}/connections/{grant_id}": {
      "delete": {
        "description": "Revokes the whole grant family for a grant made by any member of this workspace — the lever a workspace owner needs when an employee leaves and their MCP client is still running. Requires `workspace.connection.revoke`.",
        "operationId": "OAuthAuthorizeController_disconnectWorkspaceConnection",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "grant_id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Disconnected."
          },
          "403": {
            "description": "Not a member, or lacks `workspace.connection.revoke`."
          },
          "404": {
            "description": "No such connection in this workspace."
          }
        },
        "summary": "Disconnect a client on behalf of the workspace",
        "tags": [
          "oauth-authorization-server"
        ]
      }
    },
    "/v1/oauth/workspaces/{workspace_id}/connections/{grant_id}/restore": {
      "post": {
        "description": "A workspace revocation is durable: the member re-running the connect flow does not undo it, so an administrator has to. This clears the block only — every token minted under the old grant stays revoked and the member still has to approve a fresh consent screen. Requires `workspace.connection.revoke`.",
        "operationId": "OAuthAuthorizeController_restoreWorkspaceConnection",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "grant_id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Revocation lifted."
          },
          "403": {
            "description": "Not a member, or lacks `workspace.connection.revoke`."
          },
          "404": {
            "description": "No such connection in this workspace."
          },
          "409": {
            "description": "The connection was not revoked by an administrator — there is nothing to lift."
          }
        },
        "summary": "Lift a workspace revocation so the member may reconnect",
        "tags": [
          "oauth-authorization-server"
        ]
      }
    },
    "/v1/session": {
      "get": {
        "operationId": "SessionController_getSessions",
        "parameters": [],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/SessionDto"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Active sessions"
          },
          "401": {
            "description": "Unauthorized"
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "List active sessions",
        "tags": [
          "session"
        ]
      }
    },
    "/v1/verification/resend": {
      "post": {
        "operationId": "VerificationController_resend",
        "parameters": [],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FeedbackResponseDto"
                }
              }
            },
            "description": "Verification email sent"
          },
          "401": {
            "description": "Unauthorized"
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Resend verification email",
        "tags": [
          "verification"
        ]
      }
    },
    "/v1/verification/verify": {
      "post": {
        "operationId": "VerificationController_verify",
        "parameters": [],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/VerifyCodeDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FeedbackResponseDto"
                }
              }
            },
            "description": "Email verified successfully"
          },
          "400": {
            "description": "Invalid or expired verification code"
          }
        },
        "summary": "Verify an email code",
        "tags": [
          "verification"
        ]
      }
    },
    "/v1/workspaces": {
      "get": {
        "operationId": "WorkspaceController_listWorkspaces",
        "parameters": [
          {
            "in": "query",
            "name": "limit",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "cursor",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ListWorkspacesResponseDto"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "List workspaces the current user belongs to",
        "tags": [
          "workspaces"
        ]
      },
      "post": {
        "operationId": "WorkspaceController_createWorkspace",
        "parameters": [],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateWorkspaceDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkspaceResponseDto"
                }
              }
            },
            "description": ""
          },
          "403": {
            "description": "Caller is a PAK (`pcft_live_*`). Workspace creation requires a human-issued session."
          }
        },
        "summary": "Create a new workspace. Cookie/JWT only — PAKs are workspace-scoped credentials and cannot create new workspaces.",
        "tags": [
          "workspaces"
        ]
      }
    },
    "/v1/workspaces/invites/accept": {
      "post": {
        "operationId": "WorkspaceController_acceptInvite",
        "parameters": [],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AcceptWorkspaceInviteDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkspaceResponseDto"
                }
              }
            },
            "description": "The joined workspace."
          }
        },
        "summary": "Accept a workspace invite by code. Cookie/JWT only — invite acceptance binds a human identity to a workspace.",
        "tags": [
          "workspaces"
        ]
      }
    },
    "/v1/workspaces/{workspace_slug}": {
      "delete": {
        "operationId": "WorkspaceController_deleteWorkspace",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Workspace deleted."
          },
          "403": {
            "description": "Caller is a PAK (`pcft_live_*`). Workspace deletion requires a human-issued session."
          }
        },
        "summary": "Delete a workspace (requires workspace.delete). Cookie/JWT only — irreversible workspace destruction needs a human-attributed audit row.",
        "tags": [
          "workspaces"
        ]
      },
      "get": {
        "operationId": "WorkspaceController_getWorkspace",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkspaceResponseDto"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Get workspace details",
        "tags": [
          "workspaces"
        ]
      },
      "patch": {
        "operationId": "WorkspaceController_updateWorkspace",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateWorkspaceDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkspaceResponseDto"
                }
              }
            },
            "description": "Workspace updated."
          },
          "400": {
            "description": "Invalid slug or display name."
          },
          "404": {
            "description": "Workspace not found."
          },
          "409": {
            "description": "Slug already taken by another workspace."
          }
        },
        "summary": "Update workspace display name, slug, or settings. Slug renames broadcast a `workspace.renamed` event so denormalised consumers (waitlist-api) sync.",
        "tags": [
          "workspaces"
        ]
      }
    },
    "/v1/workspaces/{workspace_slug}/api-keys": {
      "get": {
        "operationId": "PlatformApiKeyController_list",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Page size. Defaults to 20, clamped to 100. Sending it (at any value) also opts the response into the `{data, pagination}` envelope.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Opaque `next_cursor` from the previous page. Sending it also opts the response into the `{data, pagination}` envelope.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "oneOf": [
                    {
                      "$ref": "#/components/schemas/ListPlatformApiKeysResponseDto"
                    },
                    {
                      "items": {
                        "$ref": "#/components/schemas/PublicApiKeyDto"
                      },
                      "type": "array"
                    }
                  ]
                }
              }
            },
            "description": "Two shapes, selected by the request — see `oneOf`.\n\n- **`?limit=` or `?cursor=` present** → the standard `{data, pagination}` envelope with `next_cursor` + `has_more`. Use this; it is the only shape that pages.\n- **neither present** → the legacy bare JSON array of every PAK in the workspace (capped at 1000). DEPRECATED as of 2026-09 and retained only so `@productcraft/platform-auth` <= 0.0.12 clients, which cannot send either query param, keep working. It will be removed once those clients are off it; new integrations must send `?limit=`."
          },
          "400": {
            "description": "Malformed `cursor`."
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "List PAKs in this workspace, newest first. Plaintext tokens are NOT returned — only prefixes + metadata. Pass `?limit=`/`?cursor=` to get the standard cursor-paginated `{data, pagination}` envelope (20/page, max 100); callers that pass neither still get the legacy bare JSON array.",
        "tags": [
          "platform-api-keys"
        ]
      },
      "post": {
        "operationId": "PlatformApiKeyController_create",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreatePakDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MintPakResponseDto"
                }
              }
            },
            "description": ""
          },
          "403": {
            "description": "Caller is a PAK (`pcft_live_*`). Mint requires a human-issued session."
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Mint a new PAK. The plaintext `token` is returned ONCE — store it now. The minter can grant at most their own effective permissions. Cookie/JWT only — PAKs cannot mint other PAKs.",
        "tags": [
          "platform-api-keys"
        ]
      }
    },
    "/v1/workspaces/{workspace_slug}/api-keys/{id}": {
      "delete": {
        "operationId": "PlatformApiKeyController_revoke",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "When `true`, publish a `pak:revoked` Redis fan-out so subscribed services evict their caches immediately instead of waiting out the 60s TTL.",
            "in": "query",
            "name": "force",
            "required": false,
            "schema": {
              "type": "boolean"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "PAK revoked."
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Revoke a PAK. Default behaviour: introspect returns null on the next upstream call; downstream services fail-closed within the 60s cache TTL window. Pass `?force=true` to additionally publish a Redis fan-out that evicts the matching cache entry across every PAK-consuming service immediately — use it when rotating a leaked PAK and the 60s window matters.",
        "tags": [
          "platform-api-keys"
        ]
      },
      "get": {
        "operationId": "PlatformApiKeyController_get",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PublicApiKeyDto"
                }
              }
            },
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Get a single PAK by id.",
        "tags": [
          "platform-api-keys"
        ]
      },
      "patch": {
        "operationId": "PlatformApiKeyController_update",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdatePakDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PublicApiKeyDto"
                }
              }
            },
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Update a PAK’s name or description (does not touch bindings).",
        "tags": [
          "platform-api-keys"
        ]
      }
    },
    "/v1/workspaces/{workspace_slug}/api-keys/{id}/bindings": {
      "patch": {
        "operationId": "PlatformApiKeyController_setBindings",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetBindingsDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PublicApiKeyDto"
                }
              }
            },
            "description": "Updated PAK with new bindings."
          },
          "400": {
            "description": "One of: bound > 25 policies; merged > 200 statements; one of the policy ids does not exist or lives in a different workspace."
          },
          "403": {
            "description": "Caller cannot grant one or more actions in the merged statement set."
          },
          "404": {
            "description": "PAK not found in this workspace."
          },
          "409": {
            "description": "Empty bindings — revoke the PAK instead of leaving it unbound."
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Replace the managed-policy bindings on a PAK. Re-runs caller-narrowing on the merged union of statements.",
        "tags": [
          "platform-api-keys"
        ]
      }
    },
    "/v1/workspaces/{workspace_slug}/audit-feed": {
      "get": {
        "description": "Merged audit timeline across the workspace surfaces. v1 covers `workspace_audit` (this service); per-app Auth and per-community Social rows arrive in a follow-up. Response shape is stable from v1 — clients can build against it today.",
        "operationId": "WorkspaceAuditFeedController_listFeed",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "ISO-8601 inclusive lower bound on `timestamp`. Items older than `since` are not returned.",
            "in": "query",
            "name": "since",
            "required": false,
            "schema": {
              "format": "date-time",
              "type": "string"
            }
          },
          {
            "description": "ISO-8601 inclusive upper bound on `timestamp`. Composes with `since`, `action`, `actor_id` and with the cursor. A bare date resolves to midnight UTC, so `until=2026-05-01` excludes that day’s later entries.",
            "in": "query",
            "name": "until",
            "required": false,
            "schema": {
              "format": "date-time",
              "type": "string"
            }
          },
          {
            "description": "Exact-match filter on the action identifier (e.g. `workspace.member.removed`). A partial string matches nothing.",
            "in": "query",
            "name": "action",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Exact-match filter on the acting account UUID. Must be a well-formed UUID — anything else is a 400, not an empty page. System rows have a null actor and are excluded by this filter.",
            "in": "query",
            "name": "actor_id",
            "required": false,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          },
          {
            "description": "Opaque cursor from a previous response's `pagination.next_cursor`. Resumes the feed at the same merge boundary.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Per-page cap. Clamped to 1..500; default 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 100,
              "maximum": 500,
              "minimum": 1,
              "type": "integer"
            }
          },
          {
            "description": "Comma-separated subset of sources to include (e.g. `workspace_audit,heimdall_audit`). Omitted = all enabled sources.",
            "in": "query",
            "name": "sources",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AuditFeedResponseDto"
                }
              }
            },
            "description": ""
          },
          "400": {
            "description": "Invalid query params — `actor_id` is not a well-formed UUID, or `cursor` is not a cursor this endpoint minted. `since`/`until` that do not parse as ISO-8601 are ignored rather than rejected, and `sources` silently drops unknown names; an empty result set after those filters still returns 200."
          },
          "403": {
            "description": "The caller is signed in but lacks `workspace.audit.read` for this workspace."
          },
          "404": {
            "description": "The `:workspaceSlug` path param does not resolve to a workspace the caller can see."
          }
        },
        "summary": "Unified workspace audit feed (cursor-paginated)",
        "tags": [
          "workspace-audit-logs"
        ]
      }
    },
    "/v1/workspaces/{workspace_slug}/audit-logs": {
      "get": {
        "operationId": "WorkspaceAuditController_listAuditLogs",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Page size. Defaults to 20, clamped to 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Opaque `next_cursor` from the previous page.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Exact-match filter on the action identifier (e.g. `workspace.member.removed`).",
            "in": "query",
            "name": "action",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Exact-match filter on the acting account UUID. System rows have a null actor and are excluded by this filter.",
            "in": "query",
            "name": "actor_id",
            "required": false,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          },
          {
            "description": "Inclusive lower bound on `created_at`. ISO-8601 date (`2026-05-01`, midnight UTC) or date-time (`2026-05-01T09:00:00Z`). Composes with `action` / `actor_id` / `until` and with the cursor.",
            "in": "query",
            "name": "since",
            "required": false,
            "schema": {
              "format": "date-time",
              "type": "string"
            }
          },
          {
            "description": "Inclusive upper bound on `created_at`. Same formats as `since`. Note a bare date resolves to midnight UTC, so `until=2026-05-01` excludes that day’s later entries.",
            "in": "query",
            "name": "until",
            "required": false,
            "schema": {
              "format": "date-time",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ListWorkspaceAuditLogsResponseDto"
                }
              }
            },
            "description": "Page of audit entries with `next_cursor` and `has_more`."
          },
          "400": {
            "description": "Malformed `cursor`, `since`, or `until`."
          }
        },
        "summary": "List workspace audit log entries, newest first. Filter by `?action=`, `?actor_id=`, and/or the `?since=` / `?until=` date range. 20/page (max 100), cursor-paginated.",
        "tags": [
          "workspace-audit-logs"
        ]
      }
    },
    "/v1/workspaces/{workspace_slug}/avatar": {
      "delete": {
        "operationId": "WorkspaceController_removeAvatar",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Avatar cleared."
          }
        },
        "summary": "Clear the workspace avatar and delete the stored object. Gated by `workspace.settings.update`.",
        "tags": [
          "workspaces"
        ]
      },
      "post": {
        "operationId": "WorkspaceController_setAvatar",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "multipart/form-data": {
              "schema": {
                "properties": {
                  "file": {
                    "description": "PNG, JPEG, or WEBP image, ≤2 MB. Square images look best.",
                    "format": "binary",
                    "type": "string"
                  }
                },
                "required": [
                  "file"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkspaceAvatarUrlResponseDto"
                }
              }
            },
            "description": ""
          },
          "403": {
            "description": "Caller is not a member of this workspace, or lacks workspace.settings.update."
          }
        },
        "summary": "Upload (or replace) the workspace identity avatar. Gated by `workspace.settings.update` (owner + admin). PNG/JPEG/WEBP only, ≤2 MB, magic-byte sniffed. SVG is rejected (stored-XSS).",
        "tags": [
          "workspaces"
        ]
      }
    },
    "/v1/workspaces/{workspace_slug}/invites": {
      "get": {
        "operationId": "WorkspaceController_listInvites",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Page size. Defaults to 20, clamped to 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Opaque `next_cursor` from the previous page.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ListWorkspaceInvitesResponseDto"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "List workspace invites, newest first. Cursor-paginated.",
        "tags": [
          "workspaces"
        ]
      },
      "post": {
        "operationId": "WorkspaceController_createInvite",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateWorkspaceInviteDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkspaceInviteResponseDto"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Create a workspace invite link. Cookie/JWT only — invites bring new humans into the workspace.",
        "tags": [
          "workspaces"
        ]
      }
    },
    "/v1/workspaces/{workspace_slug}/invites/{invite_id}": {
      "delete": {
        "operationId": "WorkspaceController_revokeInvite",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "invite_id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Invite revoked."
          }
        },
        "summary": "Revoke a workspace invite. Cookie/JWT only.",
        "tags": [
          "workspaces"
        ]
      }
    },
    "/v1/workspaces/{workspace_slug}/members": {
      "get": {
        "operationId": "WorkspaceController_listMembers",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Page size. Defaults to 20, clamped to 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Opaque `next_cursor` from the previous page.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Case-insensitive substring matched against the member’s display name, username OR primary email. `%` and `_` are matched literally.",
            "in": "query",
            "name": "q",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Exact role name the membership is bound to — `owner`, `admin`, `member`, or a custom role’s name. Composes with `q` and with the cursor.",
            "in": "query",
            "name": "role",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ListWorkspaceMembersResponseDto"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "List workspace members, oldest join first. Narrow with `?q=` (name / username / email substring) and/or `?role=`. Cursor-paginated.",
        "tags": [
          "workspaces"
        ]
      }
    },
    "/v1/workspaces/{workspace_slug}/members/{account_id}": {
      "delete": {
        "operationId": "WorkspaceController_removeMember",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "account_id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Member removed."
          }
        },
        "summary": "Remove a member from a workspace (or self-leave)",
        "tags": [
          "workspaces"
        ]
      }
    },
    "/v1/workspaces/{workspace_slug}/members/{account_id}/role": {
      "patch": {
        "operationId": "WorkspaceController_updateMemberRole",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "account_id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateMemberRoleDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UpdateMemberRoleResponseDto"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Update a member's role. Body: { roleId } or { role: \"owner\"|\"admin\"|\"member\" }.",
        "tags": [
          "workspaces"
        ]
      }
    },
    "/v1/workspaces/{workspace_slug}/policies": {
      "get": {
        "operationId": "WorkspacePolicyController_list",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Page size. Defaults to 20, clamped to 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Opaque `next_cursor` from the previous page.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ListManagedPoliciesResponseDto"
                }
              }
            },
            "description": "Page of managed policies with `next_cursor` and `has_more`."
          },
          "400": {
            "description": "Malformed `cursor`."
          }
        },
        "summary": "List managed policies in a workspace, alphabetically. 20/page (max 100), cursor-paginated.",
        "tags": [
          "workspace-policies"
        ]
      },
      "post": {
        "operationId": "WorkspacePolicyController_create",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateWorkspacePolicyDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ManagedPolicyResponseDto"
                }
              }
            },
            "description": "Policy created."
          },
          "409": {
            "description": "Name already in use."
          }
        },
        "summary": "Create a managed policy. Cookie/JWT only — policy authoring is a human-admin surface.",
        "tags": [
          "workspace-policies"
        ]
      }
    },
    "/v1/workspaces/{workspace_slug}/policies/actions/catalog": {
      "get": {
        "operationId": "WorkspacePolicyController_catalog",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkspaceActionCatalogResponseDto"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "List every known workspace action (catalog of statement actions).",
        "tags": [
          "workspace-policies"
        ]
      }
    },
    "/v1/workspaces/{workspace_slug}/policies/{policy_id}": {
      "delete": {
        "operationId": "WorkspacePolicyController_delete",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "policy_id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Policy deleted."
          },
          "404": {
            "description": "Policy not found in this workspace."
          }
        },
        "summary": "Delete a managed policy. Cascades: API keys lose the binding (deny-all if no other bindings remain); roles fall back to their inline policy column. Cookie/JWT only.",
        "tags": [
          "workspace-policies"
        ]
      },
      "get": {
        "operationId": "WorkspacePolicyController_get",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "policy_id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ManagedPolicyDetailResponseDto"
                }
              }
            },
            "description": "Policy detail. `binding_count.{pak,role}` reports how many things are currently bound — surface this in delete-confirm UX."
          },
          "404": {
            "description": "Policy not found in this workspace."
          }
        },
        "summary": "Get a managed policy by id",
        "tags": [
          "workspace-policies"
        ]
      },
      "patch": {
        "operationId": "WorkspacePolicyController_update",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "policy_id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateWorkspacePolicyDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ManagedPolicyDetailResponseDto"
                }
              }
            },
            "description": "Updated policy."
          },
          "400": {
            "description": "Invalid policy shape, name, or condition block."
          },
          "403": {
            "description": "Caller cannot grant one or more requested actions."
          },
          "404": {
            "description": "Policy not found in this workspace."
          },
          "409": {
            "description": "Name already in use."
          }
        },
        "summary": "Update a managed policy. Cookie/JWT only — policy authoring is a human-admin surface.",
        "tags": [
          "workspace-policies"
        ]
      }
    },
    "/v1/workspaces/{workspace_slug}/roles": {
      "get": {
        "operationId": "WorkspaceRoleController_list",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Page size. Defaults to 20, clamped to 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Opaque `next_cursor` from the previous page.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ListWorkspaceRolesResponseDto"
                }
              }
            },
            "description": "Page of roles with `next_cursor` and `has_more`."
          },
          "400": {
            "description": "Malformed `cursor`."
          }
        },
        "summary": "List roles in a workspace. System roles first, then alphabetical. 20/page (max 100), cursor-paginated.",
        "tags": [
          "workspace-roles"
        ]
      },
      "post": {
        "operationId": "WorkspaceRoleController_create",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateWorkspaceRoleDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkspaceRoleResponseDto"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Create a new custom role. Cookie/JWT only — role authoring is a human-admin surface.",
        "tags": [
          "workspace-roles"
        ]
      }
    },
    "/v1/workspaces/{workspace_slug}/roles/{role_id}": {
      "delete": {
        "operationId": "WorkspaceRoleController_delete",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "role_id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Custom role deleted."
          }
        },
        "summary": "Delete a custom role (fails if it has members). Cookie/JWT only — role authoring is a human-admin surface.",
        "tags": [
          "workspace-roles"
        ]
      },
      "get": {
        "operationId": "WorkspaceRoleController_get",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "role_id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkspaceRoleResponseDto"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Get a role + its permissions",
        "tags": [
          "workspace-roles"
        ]
      },
      "patch": {
        "operationId": "WorkspaceRoleController_update",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "role_id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateWorkspaceRoleDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkspaceRoleResponseDto"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Update a role or replace its permission set. Cookie/JWT only — role authoring is a human-admin surface.",
        "tags": [
          "workspace-roles"
        ]
      }
    },
    "/v1/workspaces/{workspace_slug}/services": {
      "get": {
        "operationId": "WorkspaceServiceController_list",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ListWorkspaceServicesResponseDto"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "List services activated on this workspace. Complete — one row per product, not paginated.",
        "tags": [
          "workspace-services"
        ]
      }
    },
    "/v1/workspaces/{workspace_slug}/services/{service}": {
      "delete": {
        "operationId": "WorkspaceServiceController_disable",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "service",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Service disabled. Settings preserved for re-enable."
          }
        },
        "summary": "Disable a service (settings are retained)",
        "tags": [
          "workspace-services"
        ]
      },
      "post": {
        "operationId": "WorkspaceServiceController_enable",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "service",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EnableWorkspaceServiceDto"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkspaceServiceStateDto"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Enable (or re-enable) a service for this workspace",
        "tags": [
          "workspace-services"
        ]
      }
    },
    "/v1/workspaces/{workspace_slug}/services/{service}/settings": {
      "patch": {
        "operationId": "WorkspaceServiceController_updateSettings",
        "parameters": [
          {
            "in": "path",
            "name": "workspace_slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "service",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "additionalProperties": true,
                "example": {
                  "default_sender": "noreply@acme.com"
                },
                "type": "object"
              }
            }
          },
          "description": "Replaces the per-service settings jsonb entirely. The body is the settings object itself (no wrapper). Service-specific shape — e.g. mail tracks default sender, social tracks ranking weights.",
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkspaceServiceStateDto"
                }
              }
            },
            "description": ""
          }
        },
        "summary": "Replace the settings jsonb for an active service",
        "tags": [
          "workspace-services"
        ]
      }
    }
  },
  "servers": [],
  "tags": []
}
