---
"@productcraft/social": minor
---

Add hidden-words moderation and negative-feedback primitives. Admin `/moderation/blocked-terms` (community lexicon), a muted-term `action` field, plus `/hides` (not-interested) and `/dismissed-suggestions` edges with their list endpoints.
